# v9.2.0 Task 9 最終 output：§ Annex B body skeleton 更新

**作成日**: 2026-05-25
**Task**: § 7 Task 9（§ Annex B body skeleton 更新）
**承認状態**: 包括承認モード適用
**完了基準**: Annex B が完全に新スキーマに置換 ✅（差分パッチとして起草完了）
**次タスク**: Task 10（§ 31 検証規律 S85〜S88 追加）

---

## 1. 主要構造変更 7 件

| # | 変更内容 | 由来 |
|---|---|---|
| 1 | `<section id="mindmap-tree">` 新規追加（参考直後） | Task 3 |
| 2 | `<section id="mindmap">` → `<section id="mindmap-radial">` rename + 詳細化 | Task 4 |
| 3 | `<section id="c-5">` 内 SVG 直線型 → 分岐型 flowchart-v2 完全置換 | Task 5 |
| 4 | `<section id="c-4">` 内 theory-detail-grid + statute-interpretation 追加 | Task 7 |
| 5 | `<div class="sub-card professor">` 内 prof-heading density-v2 化 | Task 8 |
| 6 | nav 全件更新（参考↔mindmap-tree↔mindmap-radial 動線追加） | Task 9 |
| 7 | footer-spec feature-tag を 33 件 v9.2.0 仕様更新 | Task 1 確定済 |

---

## 2. nav 更新マップ

| section | v9.1.0 sec-nav | v9.2.0 sec-nav |
|---|---|---|
| basis | `↑記述[N] / ↓マインドマップ` | `↑記述[N] / ↓マインドマップツリー` |
| **mindmap-tree（新規）** | — | `↑参考 / ↓マインドマップ放射` |
| mindmap-radial（旧 mindmap） | `↑参考 / ↓C-1` | `↑マインドマップツリー / ↓C-1` |
| c-1 | `↑マインドマップ / ↓C-2` | `↑マインドマップ放射 / ↓C-2` |
| c-4 | `↑C-3 / ↓C-5` | （不変） |
| c-5 | `↑C-4 / ↓C-6` | （不変） |

---

## 3. 残課題確定（Task 3/4 から繰越）

### 3-1. T1：§ 22-tree で階層 5+ の縦軸圧縮

階層数 5 以上の場合、各層の y 間隔を 100px → 75px に圧縮。viewBox 高さは "0 0 1100 800" 維持。

### 3-2. T2：§ 22-tree で同一階層 10+ ノードの二段配置

最大ノード数/層 が 10 以上：viewBox を "0 0 1300 700" に拡張（広幅 + 縦余裕）。二段配置は採用しない（推奨案 A）。

### 3-3. T3：§ 22-tree で複数論点の表示

- 2 論点まで：論点枠を 2 個並べて配置（縦並び）
- 3 論点以上：論点枠 1 個内にリスト化（`<tspan dy="1.2em">` 改行）
- 各論点が異なる L3 ノードを指す場合、各々 issue-arrow を引く

### 3-4. R1：§ 22-radial で枝あたりサブノード上限

最大 3 個（条文 1 + 判例 1 + 要件解説 1）。4 個以上必要なら §17-ter（学説対立）に展開を移行。

### 3-5. R2：§ 22-radial で空枝の処理

該当無し（特別法均衡が無い等）の枝も表示する。本文に「該当なし」「—」を記載。8 枝固定の視覚的安定性を維持。

### 3-6. R3：§ 22-radial で構成要件 5+ の追加枝

構成要件①〜④に圧縮（推奨案 A）。5 個以上は合体可能な要件を統合。8 枝構成の視覚的バランス維持。

---

## 4. body skeleton 差分パッチ概要

差分パッチ詳細（diff 形式）は本セッションのチャット欄を参照。主要変更：

### 4-1. 参考 section の sec-nav 更新

```diff
- ↓マインドマップ
+ ↓マインドマップツリー
```

### 4-2. mindmap-tree section 追加（参考直後・Task 3 SVG 骨格使用）

### 4-3. mindmap section → mindmap-radial rename（Task 4 SVG 骨格使用）

```diff
- id="mindmap"
- viewBox="0 0 1100 900"
+ id="mindmap-radial"
+ class="radial-svg"
+ viewBox="0 0 1200 1000"
```

### 4-4. C-1 sec-nav 更新

```diff
- ↑マインドマップ
+ ↑マインドマップ放射
```

### 4-5. C-4 section に theory-detail-grid + statute-interpretation 追加（Task 7）

cmp-table-wrap の直後に挿入。

### 4-6. C-5 section の SVG 全面置換（Task 5）

```diff
- viewBox="0 0 720 700"（直線型 stepbox）
+ class="flow-svg" viewBox="0 0 900 [600|800|1000]"（分岐型）
+ START + decision diamond + 肢マーカー + 終端ノード構造
```

### 4-7. sub-card.professor 内構造刷新（Task 8）

```diff
- <h3 class="prof-heading">[各見出し]</h3>
- <p>[内容]</p>
+ <div class="prof-heading prof-point">[新構造]</div>
+ <div class="prof-heading prof-process"><ol class="process-steps">...</ol></div>
+ <div class="prof-heading prof-image">[3 要素]</div>
+ <div class="prof-heading prof-application"><div class="syllogism">...</div></div>
```

### 4-8. footer-spec 全面更新

33 件 feature-tag（Task 1 確定済リスト）。

---

## 5. 完了基準充足確認

| 基準 | 状態 |
|---|---|
| Task 3/4/5 SVG スケルトン Annex B 埋込指示 | ✅ |
| 旧 § C-5 SVG 置換指示 | ✅ |
| nav 全件更新マップ | ✅ |
| footer-spec feature-tag 更新指示 | ✅ |
| 残課題 T1-T3 / R1-R3 確定 | ✅ |

引継ぎ書 Task 9 完了基準「Annex B が完全に新スキーマに置換」を差分パッチレベルで満たします（実際の Annex B 完全本文は Task 11 で起草）。

---

**END OF TASK 9 OUTPUT**
