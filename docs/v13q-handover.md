# §v13q 一括改訂バッチ 引き継ぎ書（2026-07-28）

> 前セッション（session_018RnBBCgcNtwjZ4Fc61wjXM）からの引き継ぎ。
> ユーザー指示：「Opus最大で、刑TXLEX425以上〜刑訴TX を改訂して。それ以外の既存ファイルは TJR の付随で」
> ＋「Opus５で処理して」（執筆エージェントは model: opus 指定）＋「20〜30本ごとに master へ即マージ（すぐ使うため）」。

## 1. §v13q とは（正典・マージ済み）

記述カードの単体完結化。正典は `docs/tx-v12.2.1-inline-lock.md` §v13q／`spec/tx-v13.1.0-loopcard-core.md` 第2項・第5項／CLAUDE.md §3。要点：
1. **✍答案圧縮**（`.tx-anscomp-line`）＝規範完全文＋条文・判例（答案調1〜2文）を **カード `.syn-orig` 末尾＋正誤表 `data-brief-mark` 末尾の2箇所に同一文**（シングルソース）。
2. **GIST 自己完結**＝結論先出し→体系的位置づけ→用語サンドイッチ→例え（🗝フックと同系）→当否締め。
3. ×記述の `.tx-stmt-fix` は**最小訂正**へ短縮（完全文の二重掲載禁止）。
4. **#basis 空箱禁止**＝中身が「↑ページ先頭へ」だけなら hidden アンカー化（実内容があれば据置）。
- ゲート＝`validate-tx-core.py` **G73**（片置き・不一致・CSS欠落=ERROR／未展開・可視空箱=WARNING）＋push 前 `check-tx-lex-engine.py`。

## 2. 進捗（全て master マージ済み・PR #38/#39/#41/#42）

- **完了**：刑TX425〜445（21本）／刑訴TX001〜080（80本）＝計101本。
- **残り**：**刑訴TX081〜179（99本）**。全て未着手・クリーン（部分改変の残骸なし）。
- 未着手判定は `grep -L 'tx-anscomp-line' outputs/ux/000_TX/002_刑事訴訟法/*_lex.html`。
- それ以外の既存ファイル（刑TX424以下・他科目）は **TJR 付随**（このバッチでは触らない）。

## 3. ツール（リポジトリに同梱済み）

- `scripts/v13q-extract.py <FILE>` … 執筆素材の抽出（answer-key・各記述の原文/現GIST/path/BASIS/フック・brief-mark・#basis 状態）。
- `scripts/v13q-inject.py <FILE> <payload.json>` … 決定論注入（CSS 区画の canonical 移植・syn-orig/brief-mark への同一文注入・fix 置換・hide_basis）。BOM/CRLF 保持・assert 付き・brief-mark 行の無い v13.0 表はカード側のみ注入（現存対象は無し＝001 は済）。
- payload 形式は v13q-inject.py の docstring 参照。**anscomp に半角二重引用符 `"` 禁止**（属性内に入るため）。

## 3-bis. 【2026-07-28 追記】残件は TJR-Q 特別枠へ移行済み

残り（刑訴TX081〜179）は **TJR の Q ストリーム（特別枠）** が自動消化する：通常の「TJR処理」1バッチごとに
**10本ずつ**（`-MaxQ` で変更可）headless（Opus 5 固定）で改訂→PASS のみ commit/push。残件ゼロで自然消滅。
エンジン＝`scripts/v13q-runner.ps1`／プロンプト＝`prompts/v13q-headless.md`／正典＝`docs/run-patterns.md` Q 節。
したがって**新規セッションでの手動継続（下記 §4）は不要になった**（大量に一気に進めたい時のオプションとして残す。
その場合は TJR と重ならないよう claim を確認するか、TJR 夜間タスクを止めてから行う）。

## 4. 実行パイプライン（確立済み・そのまま踏襲）

1. ブランチ：`git fetch origin master && git checkout -B <作業ブランチ> origin/master`（作業ブランチ名は新セッションの指定に従う）。
2. **執筆エージェントを `model: opus` 指定で並列投入**（同時上限20体）。プロンプトは
   「まず docs/v13q-handover.md の §5 執筆レシピを Read し、厳密に従って <FILE> を §v13q へ改訂せよ。git commit/push 禁止。報告は10行以内」。
3. 完了通知ごとに：`validate-tx-core.py` の ALL PASS を確認 → **1問ずつ commit**（コミットメッセージ例
   `feat(刑訴TX081): §v13q 改訂（答案圧縮・GIST自己完結）`）→ push。
   **注意：シェルの cwd が勝手にリセットされることがある＝毎回 `cd /home/user/bar-exam` を付ける。**
4. **20〜30本たまるごとに PR→即マージ**（ユーザーがすぐ使うため）→ ブランチを master から切り直し。
5. エージェントが session limit 等で死んだら：`git status` の変更ファイルを validate し、
   **PASS＋anscomp>3 のみ救済 commit・それ以外は `git checkout --` で破棄**（部分状態を残さない）。
6. 全て完了したら `check-duplicates.py outputs` を通し、最終報告。

## 5. 執筆レシピ（エージェント用・厳守）

対象ファイルはプロンプト末尾で指定される。**git の add/commit/push は絶対にしない**（復旧用 `git checkout -- <FILE>` のみ可）。対象ファイル以外を編集しない。作業ディレクトリは /home/user/bar-exam。

1. 素材抽出：`python3 scripts/v13q-extract.py <FILE>`
2. **全記述分を執筆**（1文1文丁寧に）：
   - **anscomp（必須）**：正しい規範の完全文を答案調1〜2文＋（条文・判例）。
     典拠は**そのファイルの path/BASIS/現GIST に書いてある条文・判例だけ**（新規判例の発明禁止・年月日表記はファイル内のまま）。`<strong>` は1〜2語。**`"` 使用禁止**（「」を使う）。×記述＝正しい規範そのものを書く（メタ文禁止）。
   - **gist（必須）**：構成＝「**○。/×。**（結論先出し）→ 体系的位置づけ（何罪・どの論点か）→ 規範（専門用語は**用語サンドイッチ**＝直後に生活語で開く。核用語 `<strong>` 1〜2語のみ）→ 理由（保護法益・趣旨）→ **イメージは◯◯——**（その記述の🗝記憶のフックと同系の例え）→ 当否（〜誤り＝×。／〜正しい＝○。で締める）」。250〜350字。
     **締めの○×は answer-key と必ず一致**。既存 `a.ref-stat` アンカー（id/href）は温存して相互リンクを切らない。
   - **fix**：×記述の `.tx-stmt-fix` が長文（目安25字超・判例引用入り）なら最小訂正（判例名は書かない）へ。既に短い／○記述／チップ無し・複数は null。
3. payload JSON をセッションの scratchpad（または /tmp）に書き、`python3 scripts/v13q-inject.py <FILE> <payload>` で注入（**対象 HTML を直接 Edit しない**）。`hide_basis` は extract が「空箱(可視)」の時だけ true。
4. ゲート：`python3 scripts/validate-tx-core.py <FILE>`（**ERROR 0 必須**）→ `python3 scripts/check-tx-lex-engine.py <FILE>`（**PASS 必須**）。ERROR が出たら `git checkout -- <FILE>` で戻し、payload 修正→再注入→再ゲート。
   - 頻出のひっかかり：**G32**（GIST に「本問」等の問題ローカル依存語＝禁止）／**G21**（禁止句「絞り込」「切れれば」等）／記述キー（ア・①等）への言及禁止。
5. 自己照合1回：①GIST 締め○×＝answer-key ②条文・判例年月日＝ファイル内記載と一致 ③記号フリー ④anscomp に `"` なし。
6. 報告10行以内：ファイル名／記述キーと○×／fix 短縮した記述／hide_basis／validate・engine 結果。

### 品質見本（gold・刑TX425/426 で確定した形）

- anscomp（×記述の例）：
  `収賄罪（刑法197条1項）にいう「職務」とは、当該公務員の<strong>一般的職務権限</strong>に属する事務であれば足り、本人が現に具体的に担当している事務であることを要しない（最判昭37.5.29）。`
- gist（全要素入りの例）：
  `×。収賄罪（刑法197条1項）は、公務員が「職務に関し」賄賂を受け取ると成立する。出発点は「職務」の広さで、判例は、<strong>一般的職務権限</strong>——その公務員が役所で本来任されている仕事の守備範囲——に属していれば「職務」にあたり、いま現に自分が担当している案件であることまでは要しないとする。担当外でも権限の範囲内なら、賄賂を受け取った時点で<strong>職務の公正に対する社会一般の信頼</strong>（公務はカネで動かないという信頼）が汚れるからだ。イメージは役所の引き出し——「職務」は今日机の上に開いているファイルだけでなく、自分の課の引き出しに入っている案件すべてに及ぶ。本記述は、判例が明示的に「要しない」と述べた「現に具体的に担当している事務であること」を必須条件に格上げし、処罰の網を判例より狭めているので誤り＝×。`
- fix 最小化の例：`→ 判例は一般的職務権限に属すれば足り、…（最判昭37.5.29）` → `→ 要しない（一般的職務権限に属すれば足りる）`

## 6. 完了後

- 本引き継ぎ書と `scripts/v13q-extract.py`／`v13q-inject.py` は残してよい（TJR 付随の既存展開でも同レシピ・同ツールを使う）。
- 380 未満の刑TX・他科目への展開は TJR 付随（このバッチの対象外）。
