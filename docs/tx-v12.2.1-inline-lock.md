# TX v12.2.1 inline 表示LOCK 正典（2026-07-01）

この文書は、TX355-360 の実地修正で合意した `_lex` 表示・操作・解説構造を、今後の既存ファイル更新と新規 TX 生成に必ず継承させるための正典である。

## 適用範囲

- 対象：`outputs/ux/000_TX/**/**/*_lex.html` のうち、`.tx-inline-card` を持つ v12 inline 型。
- 起点：`canonical/GENESIS-CORE.html`。
- 検証：`scripts/validate-tx-core.py` G45 と `scripts/check-tx-lex-engine.py`。
- 関連正典：`spec/tx-v12.1.0-inline-core.md`、`docs/tx-lex-two-track.md`、`docs/canonical-lineage.md`。

## 最終合意チェックリスト

更新・新規生成では、少なくとも次の状態を満たすこと。

- 問題文と○×ボタンは `.tx-inline-card` 内で一体化する。○/×ボタンは右端に寄せ、本文を不自然に圧迫しない。
- カード内の回答比較バッジは、○×ボタンの下に `あなた ×` / `正解 ○` の2行で表示する。`自分` 表記にはしない。バッジは薄い枠だけにせず、重厚な枠・影・背景を持たせる。
- 回答操作パネル内に大きな `正解/不正解`、`あなたの答え`、`正解`、`相違` のサマリー箱を出さない。
- トーストは使わない。回答後フィードバックは、カード内バッジと解説冒頭の比較用正誤表に集約する。
- 解説冒頭の比較用正誤表は、`あなた` 列を持たせる。ただし詳細解説表ではなく、各肢1文の「登場した論点のコア（転用可能な法理）」だけに圧縮する。
- 詳しい `文言・趣旨・射程・切断点・転用` は、表に詰め込まず、各設問の解説本文側に残す。
- 解法ナビは問題文直下、インラインカード前に置く。ヒントは結論・正誤・正解番号を漏らさず、「どこを見るか」を示す。
- 条文・判例ボックスは、見出しチップ、項ラベル、本文2カラム、本文先頭1字下げを維持する。判例も同じ扱いにする。
- 物語解説は、`解答を表示` または `解説だけ閲覧` の前に露出させない。表示後はラベル＋題名カードで、本文と被らない通常フローに置く。
- 文章本文の段落先頭は1字分空ける。ラベルやバッジに字下げを掛けず、本文列全体を余白で押し込まない。
- 詳説最下部の条文・判例本文は `BASIS` ボックスに収める。
- 凡例の `論` は「論点」ではない。論文試験と短答試験で重なる重要箇所に引くロンマークであり、凡例文言は `論文と重複` に固定する。
- 解説内容は機械的に確定しない。最新法令・判例・学説と、事案対応・条文番号・項・判例射程を最高エフォートで確認する。

## 画面導線

- 問題文と ○× は一体化する。各記述は `.tx-inline-card[data-stmt]` に置き、○/× ボタンは `.tx-inline-actions` としてカード右端のバランスよい位置に寄せる。
- 裏の `.answer-ox-grid` は Lexia/SM2・answer-key・検証用のデータ源として DOM に残すが、画面の主導線に戻さない。
- `解法ナビ` は問題文の直下、インラインカード群の前に置く。空の飾り枠にせず、検討順・現在の設問・次の操作を読める状態にする。
- 解法ナビのヒントはネタバレ禁止。正誤・結論・正解番号を出さず、「どの客体を見るか」「条文上どの要件に当てるか」「判例のどの射程に注意するか」のような注目点にする。
- 解法ナビの各 `STEP` / `STMT` には、`tip` だけでなく解答前表示用の `hint` を必ず置く。`hint` 欠落時のフォールバックに頼らず、各記述固有の分岐点を1つ以上入れる。
- 解法ナビ内に `.sn-answer-choices` を出し、現在STEPの○/×をその場で選べるようにする。下の一問一答へ移動しないと回答できない導線は禁止。
- `.sn-nav-ox[data-stmt][data-value]` は裏の `.answer-ox-grid .ox-row[data-stmt] .ox-btn[data-value]` をクリックして同期する。ナビ上の回答、下の一問一答、採点ボタンの有効化がズレてはならない。
- 全記述を選んだ後の `解答を表示` ボタンを欠落させない。`解説だけ閲覧` は answered を付けず、SM2 記録を発生させない。
- 旧 `FINAL ANSWER` 表は inline 画面に表示しない。`data-answer-key` などの抽出用 DOM としては残してよい。

## 条文・判例ボックス

- 各肢の `ANSWER` 箱の直後に、必ず `.tx-mini-law` を置く。文言・趣旨・射程だけで根拠を代用しない。
- 条文カードはブルー系、判例カードは `.is-case` のピンク系。上段チップはすべて重厚な楕円ピルにする。長方形・薄い枠だけのラベルへ戻さない。
- 上段チップに `題名：`、`テーマ：` という説明語を表示しない。
- 題名チップは下部 `basis-card-header` の参考条文・判例タイトルから取る。条文は罪名・制度名、判例は判例名や括弧内タイトルを使う。
- テーマチップは法理名にする。単なるカテゴリ名ではなく、例として `現住性錯誤`、`公共危険の非限定説`、`建造物の一部性` のように判断規範を示す。
- 条番号チップ（`.tx-mini-law-article`）は **ASCII 短縮形**にする（例 `112条` / `109条1項・2項`、判例は `最決平元.7.14`）。**漢数字（`第百十二条`）にしてはならない**。題名・法理テーマの導出はエンジンが ASCII 条番号の正規表現で行うため、漢数字だと導出が全滅し、本文を16字で切ったゴミチップ（例 `第百八条及び第百九条第一項の罪の`）が出る。本文（条文原文 `.tx-mini-law-body` / `.tx-mini-law-text`）は正式な漢数字のままでよい。`validate-tx-core.py` G45 が漢数字チップを検出する。
- 本文冒頭の `本条` / `本文` / `①` / `②` / `判旨核心` / `判例` は `.tx-mini-law-para` で小ラベル化する。素文字にしない。
- 条文の項番号ラベル `①` / `②` は、2項以上ある条文でのみ使う。単一項または条文全体を示す場合は `本条` / `本文` を使う。
- `.tx-mini-law-para` を使う行は、本文を `.tx-mini-law-body` で包み、ラベル列＋本文列の2カラムにする。判例ボックスも同じ構造にする。
- 字下げの定義：段落本文の先頭文字だけを1字分空ける。折返し行は本文列にそろえ、ラベル下へぶら下げない。余分な予約スペースを作らない。
- 判例本文の先頭に `:` など不要な記号を入れない。

## 論点処理マトリクス・5点フロー・コツ・記憶フック

- 論点処理の「整理図」は、各肢の `.tx-logic-matrix`（4セル処理順＋判断式）か、問題単位の `.tx-sysmap`（体系マップ）のどちらかで置く（validate G45 はどちらか一方があればよい）。5記述が同一の体系（限定列挙・所有区分など）を突く問題は、記述ごとに似たマトリクスを5枚並べるより、**体系マップ1枚に畳んで各記述を枝に対応づける**方がよい（情報整理・周回速度・転移の点で優位）。
- `.tx-logic-matrix` を採る肢では全肢に入れ、条文判例の直下・5点フローの直上に置く。ある肢だけ欠ける・条文判例より上・5点フローより下の配置崩れは禁止。`.tx-sysmap` を採る問題ではマトリクスを置かなくてよい。
- **整理図は焼き直し禁止（最重要）**：`.tx-logic-matrix` の各セル `.tx-matrix-body` を `.tx-flow-body`（5点フロー本文）の逐語コピーにしない（同一24字以上の連続一致で G45 ERROR）。判断式 `.tx-matrix-verdict` と記憶フック `.tx-op-body` を同一文にしない。マトリクスは「見る軸 → 確認事実 → 切る場所 → 結論到達」の上位地図、判断式は結論到達の決定式、記憶フックは1秒で思い出す標語に**役割分離**する。gold 見本＝刑TX366（逐語重複 1/20 以下）。
- `.tx-sysmap`（体系マップ）は記述リストの一番上（解説の先頭・記述1の直前）に置き、**解く間は非表示**にする。表示は `#part-a:has(.tx-inline-explain:not([hidden])) .tx-sysmap`（＋`[data-browse-open="true"]` フォールバック）の CSS フックで、「解答を表示／解説だけ閲覧」時に現す（エンジンJSは無改変＝接ぎ木禁止）。各記述タグ（`a.tx-sysmap-probe` → `#stmt-N`）と各解説末尾の戻りリンク（`.tx-sysmap-back` → `#tx-sysmap`）でハブ往復させる。見出しラベル（`.tx-sysmap-section-label`＋番号バッジ）・ノードのヘッダー帯・付属犯リストの交互背景（ゼブラ）・テーマカラー（`--accent`/`--mid`）で重厚に作る。gold 見本＝刑TX360。
- 文言・趣旨・射程・切断点・転用の各ブロックは、ラベル列＋本文列の2カラムにする。
- 2カラム化された段落でも、本文先頭だけ1字分字下げする。折返し行は本文列にそろえる。
- `コツ` や `記憶フック` はラベルと本文を分け、ラベルだけが左にぶら下がる表示にしない。
- `コツ` は結論を言わない。問題文のどこに注目して考えるかを示す。
- `記憶フック` は解説要約ではない。論点のコア・テーゼを15〜45字程度の一言にし、55字を超える判例名・要件・結論の連結文へ戻さない。

## 解説品質レビュー

- 解説は TX の学習価値そのものであり、機械的な整形・一括置換・テンプレ埋めだけで確定してはならない。
- 新規生成・既存更新・一括伝播のいずれでも、公開・push 前に、作業時点で利用可能な最上位モデルを使い、推論/エフォート設定は最大にして内容レビューを行う。
- 機械チェックは構造・表示・欠落・ネタバレ・旧UI残存の補助検査に限る。`validate-tx-core.py` や `check-tx-lex-engine.py` が PASS しても、解説内容の正確性が保証されたとは扱わない。
- レビュー対象は少なくとも、`ANSWER`、`.tx-mini-law`、`.tx-logic-matrix`、`.tx-article-flow`、`コツ`、`記憶フック`、`詳説`、`物語解説`、SM2 用 `.ox-pool-explain` とする。
- レビューでは、問題文の事実、条文番号・項、判例名・判旨、結論、文言/趣旨/射程/切断点/転用の対応、正誤理由、非ネタバレ性を個別に照合する。
- 薄い一般論、結論の言い換えだけのコツ、条文・判例根拠の欠落、事案と条文の客体取り違え、軽い罪/重い罪の錯誤、現住/非現住などの法的分類ミスは、表示が整っていても不合格とする。
- 内容レビューが未了のファイルは、v12.2.1 完了・配信可・push可とは呼ばない。

## 最新法令・判例・学説レビュー

- TX は司法試験対策教材なので、生成・更新時点の最新法令、最新判例、主要学説の現状を必ず確認する。
- 法令は e-Gov 法令検索（`https://laws.e-gov.go.jp/`）を一次確認先とし、現行条文、未施行改正、施行日、改正前後の文言を確認する。
- 司法試験・予備試験の出題基準日は、法務省の当該年度の司法試験/予備試験実施情報、Q&A、試験用法文登載法令を確認する。教材の基準日は「生成日現在」ではなく、対象試験年度の法令基準に合わせる。
- 判例は裁判所の裁判例検索（`https://www.courts.go.jp/hanrei/search1/index.html`）と最近の裁判例（`https://www.courts.go.jp/hanrei/index.html`）を確認する。裁判所サイトは全判決を網羅しないため、最高裁判例集、判例百選、重要判例解説、主要判例データベース等で補完する。
- 学説は公式の単一ソースがないため、最新版の基本書、判例百選、重要判例解説、法学教室・法学セミナー等の最新解説、受験年度向け改正情報を確認する。
- 旧法・旧判例・旧説に基づく過去問を扱う場合でも、現在法での処理を必ず確認し、必要なら `tx-current-law-note` コラムを入れる。
- `tx-current-law-note` には、確認日、参照ソース、旧法/旧判例/旧説、現在法/最新判例/現在の有力説、立法経緯・改正経緯、改正趣旨、試験対策上の扱いを短く書く。
- `tx-current-law-note` の中身は `.tx-law-note-row` で、左に `.tx-law-note-label`、右に `.tx-law-note-body` を置く2カラムにする。ラベル列は `grid-template-columns:max-content minmax(0,1fr)` で実幅に寄せ、本文との間を空けすぎない。ラベルは字下げせず、本文側だけ各段落の先頭を1字下げする。
- 新判例や改正で結論・理由付け・用語が変わる場合は、本文を最新化し、旧処理は「旧法下の過去問理解」として補足に退避する。新旧どちらで答えるべきか曖昧な状態で配信しない。
- 最新確認が未了のファイルは、内容レビュー未了と同じく、v12.2.1 完了・配信可・push可とは扱わない。

## 物語解説

- 物語解説は初回理解用の読み物であり、通常の周回主導線ではない。
- `解答を表示` または `解説だけ閲覧` の前に物語解説を露出させない。
- reveal / browse 後は、インライン解説側にも物語解説が出るようにする。
- 物語解説の各ブロックは、ラベルと題名を持つカードにする。色はその問題の配色パターンに従う。
- `data-fa-label` ラベルは通常フロー内で左寄せにする。`position:absolute` や `left:` で浮かせない。本文と重ねない。
- ラベル付き段落の本文は必ず `.fa-narrative-body` に包み、本文側だけ `text-indent:1em` で1字下げにする。ラベル・見出し・バッジは字下げ対象ではない。
- 強調語は太くしすぎない。`.fa-narrative b` は `font-weight:560` 以下を維持する。

## 記憶フック・回答フィードバック

- 記憶フック `.tx-onepoint .tx-op-body` は本文列全体を `padding-left` で押し込まない。本文先頭だけ `text-indent:1em` で1字下げにする。
- `解答を表示` 後、回答操作パネル内に大きな「あなたの答え／正解／相違」サマリー箱を出さない。回答比較はカード内判定欄と解説冒頭の正誤表に集約する。
- 問題カード内の判定欄は、○×ボタンの下に2行で出す。本文列を圧迫しないため、`あなた ×` / `正解 ○` のように結論記号だけを短く表示し、バッジは重厚な枠・影・背景で視認性を確保する。
- 解答後・解説だけ閲覧後は、解説冒頭に `.statement-verdict-table` を複製した正誤表を常時表示し、`あなた` 列を追加してユーザー回答と正解を比較できるようにする。
- 解説冒頭に表示する正誤表は詳細解説ではない。`文言・趣旨・射程・切断点・転用` の全行をそのまま出さず、各肢1文の「登場した論点のコア（転用可能な法理）」に圧縮する。詳しい内容は各設問の解説本文に残す。
- トーストは廃止する。回答後フィードバックは、解説冒頭の正誤表とカード内2行判定に集約する。
- ユーザー回答が正解の行は緑背景、誤答の行はピンク背景、未回答は淡色で表示する。

## 詳説・BASIS

- `詳説を開く` は空展開禁止。`details.tx-inline-detail` には必ず `.tx-detail-panel[data-partb-source]` を置く。
- 詳説最下部の条文・判例本文は、`BASIS` タブ付きの `sub-card basis-link` ボックス内に収める。枠・背景・タブを CSS リセットで消さない。

## 確認サーバー

- ローカル確認は `outputs/ux` をルートにして HTTP サーバーで見る。ファイル直開きでは相対パス・ブラウザ挙動が本番とずれるため、確認 URL を使う。
- 例：`python -m http.server 8788 --bind 127.0.0.1` を `outputs/ux` で起動し、`http://127.0.0.1:8788/000_TX/001_刑法/刑TX355_lex.html` を開く。
- この URL はこの PC 内だけで見える確認用 URL であり、公開 URL ではない。

## 必須ゲート

更新・生成後は少なくとも次を通す。

```powershell
python -X utf8 scripts/validate-tx-core.py canonical/GENESIS-CORE.html
python -X utf8 scripts/validate-tx-core.py canonical/GENESIS-CARD.html
python -X utf8 scripts/validate-tx-core.py outputs/ux/000_TX/001_刑法/刑TX355_lex.html
python -X utf8 scripts/validate-tx-core.py outputs/ux/000_TX/001_刑法/刑TX356_lex.html
python -X utf8 scripts/validate-tx-core.py outputs/ux/000_TX/001_刑法/刑TX357_lex.html
python -X utf8 scripts/validate-tx-core.py outputs/ux/000_TX/001_刑法/刑TX358_lex.html
python -X utf8 scripts/validate-tx-core.py outputs/ux/000_TX/001_刑法/刑TX359_lex.html
python -X utf8 scripts/validate-tx-core.py outputs/ux/000_TX/001_刑法/刑TX360_lex.html
python -X utf8 scripts/check-tx-lex-engine.py outputs/ux/000_TX/001_刑法
python -X utf8 -m py_compile scripts/validate-tx-core.py scripts/check-tx-lex-engine.py
```

`check-tx-lex-engine.py` は G41/G42/G43/G44＋G50-G60（v13）＋G61/G62（v13n）＋G63（カード⇄プール三点整合）＋G64（バッジ⇄key矛盾＝integrity L1 三層化）＋SNTIP＋判例元号割れ＋CORE/CARD 両スロット契約＋CSSドリフト可視化（tx-lex-css-canonize --check・非ブロッキング）を横断実行し、v12.2.1 **または v13 LOOP-CARD** マーカー付き・明示指定ファイルには G45 も適用する（2026-07-11 監査で v13 が G45/G61/G62 の検査対象外だった穴を恒久修正）。接ぎ木、組合せ当否、空詳説、回答UI崩れ、表示LOCK崩れ、v13n 崩れを push 前に止める。`canonical/GENESIS-CARD.html` 自身も `_lex` 同様に G23/G25 期待値＋G30-G64 の検査対象（validate-tx-core の `is_lex_target()`）で、正典をステージした commit は pre-commit（validate-staged・block 既定）でも自動検査される。

---

## v13k 表見出し中央揃え＋コツ箱ぶら下がり修正（2026-07-04・ユーザー訂正の正典化）

実地レビューで判明した v13 LOOP-CARD `_lex` の 2 表示欠落を正典化した。伝播・回帰防止まで
含めて 1 セット（正典 CSS／エンジン＝作らせない、ゲート＝作っても弾く）。

1. **表の見出しセルは中央揃え（本文データ td は左のまま）＝ `.cross-column th` と同概念。**
   静的正誤表 `.tx-sysmap-cross` の `thead th`（記述／問われた軸／結論・決め手）と `tbody th`
   （記述番号）に `text-align:center; vertical-align:middle` を与える。JS 正誤表
   `.statement-verdict-table thead th`・`.freq-badge` も中央。字間は `letter-spacing:normal`
   で均等（cramped な独自トラッキングを置かない）。CSS ブロックは `<style>` の v13g 表節直後に
   `/* === v13k: 表の見出しセル… === */` として置く（gold 刑TX359／`canonical/GENESIS-CARD.html`
   に実装。`tx-lex-css-canonize.py --check` が 35 問の共通 CSS 一致で回帰を弾く）。

2. **解法ナビ『💡 コツ』箱の本文は `.sn-tip-b` で包む（ぶら下がり禁止）。**
   旧世代の solve-nav `renderStep()` は本文 `s.tip` を生挿入していたため、`.sn-tip{display:grid}`
   の子が `<b>` 等のインライン断片ごとに分裂し、強調語が左端へ落ちる「ぶら下がり」が出た。
   正典 `canonical/SOLVE-NAV.html`／`GENESIS-CARD.html` は既に `<span class="sn-tip-b">…</span>`
   で包む形。既存 `_lex` を同形へ揃えると、本文が 1 個の grid 子（2 列目）に収まり、
   `.sn-tip .sn-tip-b{text-indent:1em}` で本文頭 1 字下げも効く。

- **伝播ツール（決定論・冪等・CRLF保存・本文不変）**：`scripts/tx-lex-v13k-labelfix.py --apply`
  （① を `.tx-sysmap-cross` アンカー越しに v13 sysmap 問へ、② を裸コツを持つ全 `_lex` 世代へ）。
- **回帰ゲート**：`check-tx-lex-engine.py` に `SNTIP` 検出を追加（`sn-tip-h">💡 コツ</span>'+s.tip+'</div>`
  の未包みを弾く）／`tx-lex-css-canonize.py --check`（v13k を含む共通 CSS 一致）。

---

## v13k-bis 体系マップSVGのラベル見出し中央化＋本文初行字下げ（2026-07-04・420で合意→全35問展開）

体系マップ（`.tx-sysmap-svg`）の各カテゴリ箱（職権/濫用/権利妨害 等）は色帯ヘッダーが左寄せ・
本文も字下げ無しだった。これを表見出し（v13k）と同概念で整える：

- **ラベル見出し中央化**：category(`font-size 18`)＋sublabel(`font-size 16`)の隣接ペアを
  1つの `<text x="0" text-anchor="middle">`＋`<tspan>` に統合＝**箱幅に依らず確実に中央**。
  サブラベルの無い単独 category（256型）は単独で `x=0 text-anchor=middle` に中央化。
- **本文初行字下げ**：各箱の本文初行（`y="58" fill="#493730" font-size="14.5"`）の**本文頭へ
  全角スペースを1つ**挿入（x座標に依存しない＝箱幅が違う089型 x=-283 でも効く・二重挿入しない）。

伝播ツール＝`scripts/tx-lex-sysmap-center.py --apply`（決定論・冪等・CRLF保存・SVG座標と本文以外は不変）。
SVGは問題固有の作画のため CSS 一括では効かず、本ツールで全35問へ展開した（089=2箱・174=見出しのみ・
256=lone・420=既編集を全角スペース方式へ統一）。検証は `validate-tx-core.py`（G45含む）＋`check-duplicates.py`。

**【追補 2026-07-16・LEX-395 文字重なりの恒久ゲート化】** 本節の展開後に生成・再生成された問題で、
親箱見出し帯を旧形（`<text>` 2本の固定 x 並置＝見出し fs18 x=-198／サブ fs16 x=-120、または中央寄せ
見出し＋左寄せサブ文の同一ベースライン重畳）で書く回帰が再発し、見出しが約 78px を超えた分だけ
2本目に文字が重なった（実害＝刑TX395_lex「職務の適法性」×「要件・現在性」30px、刑TX125_lex は完全
重畳、公式 刑TX293.html を含む計 10 ファイル/16 件→全是正）。原因＝センター化はツールがあるだけで
**検出ゲートが無く**、生成後に誰も走らせなければ旧形のまま出荷されること。恒久対策の三層＝
①placeholder 契約に「見出し帯は 1 `<text>`＋`<tspan>`・2本並置禁止」を明記（作らせない）／
②`validate-tx-core.py` **G69**（同一行の `<text>` 重なり・幾何判定＝`tx_sysmap_geom.iter_text_collisions`・
6px 超=ERROR／2〜6px=WARNING）／③push 前 `check-tx-lex-engine.py` に G69 組込み（弾く）。
修復＝`scripts/tx-sysmap-fit.py`（重なりを含む sysmap の見出し帯ペアを幾何判定で本節の正典形へ統合・
fs15 サブ見出しや公式 000_TX にも効く・冪等・CRLF 保存）。

---

## v13m 統合解説の平易化（やさしい版）＋🗝記憶のフック＋横串・誤解の罠（2026-07-06・合意の正典化）

v13 LOOP-CARD `_lex` の**記述カード解説の書き方**（＝`.sub-card.synthesis` と `.tx-v13-trap` の
**中身の執筆規約**）を正典化する。**DOM/CSS/JS・スロット構造・第5項の物理順は不変**（PLACEHOLDER-LOCK
順守）で、これは「どのスロットに何をどう書くか」の内容規律。初例＝**刑TX362_lex**（放火・全5記述で実装済み）。

**A. `.syn-lead`（💡THE GIST）＝やさしい版（用語サンドイッチ）**
- 専門用語は**直後に生活語で開く**（例：観念的競合＝「1個の行為で2つの罪が立つ」／保護法益＝「守るもの」／
  具体的危険犯＝「危険が現実に出て初めて成立」）。中高生でも詰まらない密度にする。
- **核になる用語は1ブロック1〜2個まで**（3語4語出す“用語スープ”禁止）。**初出の用語だけ太字**（`<strong>`）にし、
  「ここが覚える語」を目で分かるようにする。
- 結論（○/×）を先出し。精密な条文・判例根拠と BASIS 参照は `.syn-path`（1/2/3）に**温存**する
  （lead＝やさしい導入／path＝精密根拠、の役割分担。重複を恐れずレイヤーで分ける）。

**B. `.syn-image` の役割＝💭INTUITION（2〜3文の比喩）→ 🗝記憶のフック（締めの一行）へ変更**
- ラベルは `<span class="syn-tag">🗝 記憶のフック</span>`。**class/DOM は `.syn-image` のまま**（既存 CSS を流用・
  構造不変）。中身は鉤括弧でくくった**一行の標語（＋必要なら補足1文まで可）**にする（例：「保険が掛かれば、
  自分の家も“他人の物”」）。**【2026-07-11 現実化】**正典・corpus の実態は「標語＋補足1文」の2文型が主流のため、
  補足1文までを正規形として明文化（2文超の説明化・解説要約への回帰は従来どおり不可）。
- 比喩の 2〜3 文は lead 側に吸収し、締めは記憶に残る 1 行に圧縮する（“盛りすぎ”＝用語スープの逆を末尾でも徹底）。

**C. `.tx-v13-trap`（⚠️間違いやすいポイント）＝cross-cut・誤解の罠を積極投入**
- **【用語・2026-07-09】本文表記は「横串」→ 英語 `cross-cut` に統一**（概念は同じ＝複数論点を1本で貫く/似た別論点の混同フラグ）。gate 側 `_TRAP_YOKO` は `横串`/`cross-cut`/`CROSS-CUT` 全て marker として認識（`validate-tx-core.py`）。既存 corpus は `scripts` 一括置換で移行済み・以後の新規は `cross-cut` で書く。
- **【表示規約・2026-07-09／G58 で機械強制】** cross-cut の書き方を2形に固定する：
  ① **リード形（罠の冒頭で cross-cut を名乗る）＝チップ**：`<span class="tx-cc-tag">🔗 CROSS-CUT</span>（罠種）：本文`。`.tx-cc-tag` CSS は **canonical/GENESIS-CARD.html に同梱**（2026-07-11・複製で自動継承・チップ未使用問では不活性＝v13n CSS と同方式。canonical 由来でない旧世代 _lex でチップを使う時だけ `<style>` に手動注入）。罠 body 先頭に来るチップは親 `.tx-v13-trap-body` に **`tx-cc-lead`**（`text-indent:0`）を足す（親の1em字下げでチップがずれるため）。旧 `横串の罠：`／`◯◯の罠（横串）：` 型は不可。
  ② **文中の名詞＝小文字インライン**：日欧混植で **`cross-cut` の前後に半角スペース**（`…で切る cross-cut。`・`cross-cut の罠`）。`。）：`等 約物の前は空けない。助詞直付き `cross-cutの`／`cross-cutで` は不可。
  回帰防止＝`validate-tx-core.py` **G58**（①助詞直付き・②チップCSS欠落・③body先頭チップの `tx-cc-lead` 欠落＝ERROR／④`（cross-cut）`括弧タグ・⑤生 cross-cut リード＝WARNING）を `check-tx-lex-engine.py` の push 前ブロック対象に追加。静的プレビュー実測は `.claude/launch.json`(static:5517)＋playwright/preview_eval で `chipOffset=0px` を確認。
- 出題者は**受験生が引っかかる・差がつくところ**を突く。そこを**先回りで潰す**罠枠を積極的に入れる（バンバン）。
- 主軸の横串＝**「民法＝観念的／刑法＝現実的」**（焼損は従物/付合物でなく「毀損しなければ取り外せるか」で判断／
  占有は民法の占有改定＝観念的取得に対し刑法は現実の占有＝強盗・詐欺・盗品等譲受は占有改定どまりで未遂）。
  **複数の罪をまたぐ一本の発想**で束ね、記憶効率を上げる。
- **「よく似ているが別論点」は混同フラグを立てる**（例：平安神宮事件＝焼損の時点ではなく、回廊接続の建物群を
  一個と見る**一体性**の話。記述をまたいで「これは別の話」と明記）。
- 既存 trap スロットを拡張するのが基本。**trap の無い記述には同一 markup の `.tx-v13-trap` を1枠新設してよい**
  （第5項の位置＝`.tx-sysmap-back` の直前。`check-tx-lex-engine.py` G43/G45 は同型 markup なら PASS＝刑TX362 で実証）。
- **正確性ガード（必須）**：判例は**年月日を一次確認**してから載せる（例：平安神宮＝最決平元.7.14／エレベーターの
  かご＝最決平元.7.7／畳・布団＝最判昭25.12.14。似た日付の取り違えに注意）。民法側で**説が割れる点は断定せず**
  「刑法はその区別を使わない」と核心だけ書く（例：雨戸が従物か付合物か）。**通説どまりは“判例”と書かない**
  （占有改定→強盗/詐欺未遂は発想として提示）。§「解説品質レビュー」「最新法令・判例・学説レビュー」に従う。

**D. マップ短縮版は作らない**——テキストの論点マップは体系マップ SVG（`.tx-sysmap-svg`）と重複するため新設しない。

**E. 帰結の書き切り＝「AではなくB」＋理由（2026-07-14 ユーザー指摘の正典化）**
- ×記述の解説を「◯◯罪は成立しない」「△△条の成否が問題となる」で**終えない**。読者は当然「じゃあ何罪に
  問われるのか」と思うので、**成立する罪名（条・項）まで書き切る**（例：刑TX378 記述3＝虚偽有印公文書作成罪は
  不成立→**有印公文書偽造罪（155条1項）が成立する**まで言い切る）。
- **なぜそちらになるかの理由づけを1〜2文、やさしい版（A と同じ用語サンドイッチ）で添える**。結論の言い換えは
  理由にならない（例：「公文書の名義人＝文書の作り手として表示されている主体は『その文書を作る権限のある者』を
  指す。だから権限のない者が本名＋実在の肩書で作っても、文書の顔と実際の作り手が別人格になる＝名義を偽る
  有形偽造」）。
- 書き切る場所＝**THE GIST（短く）／`.syn-path` あてはめ・帰結（理由の本体）／`.ox-pool-explain`
  （プールカードは単独表示されるため必須）**。BASIS「本問への帰結」・trap にも一言添えると往復読みに耐える。
- **不可罰（無罪）が正しい帰結の場合は「不可罰」と明示**し、収まりを良くするために罪名を捏造しない。
  罪名を断定する際は §「解説品質レビュー」(c) の的絞りWeb確認で法理を裏取りする。
- 機械化困難のため執筆者本人の自己照合で担保（A と同様・author-once）。初例＝刑TX378 記述3（公式・_lex 両系統）。
- **既存展開（2026-07-14 完了）**：380-445 の全66ファイルを機械プレスキャン→フラグ30ファイルを全記述監査し、
  25問を修正（成立罪名＋理由の書き切り・不可罰型11問前後を含む）・4問（396/407/415/437）は書き切り済みを確認。
  付随発見＝刑TX381 のプールgist極性表示（×答え3記述の末尾「正しい（○）」固定）を是正。プールgist の明示○×
  ラベルは G60 の照合対象外なので、執筆時は「gist の締めラベルが answer-key と同じ向きか」も自己照合に含める。
  **380-445 以外の既存ファイルは TJR 付随で消化**（ユーザー指示）。

**検証・展開**
- 構造不変なので既存ゲートで回帰検知：`validate-tx-core.py`（G45＋**G51-G54＝BASIS中身/横串trap/相互リンク/🗝記憶のフック**）＋
  `check-tx-lex-engine.py`（G41〜G45＋**G50-G54 を push 前に横断検査**）＋`check-duplicates.py`。
  内容の正確性（THE GIST＝やさしい版・§v13m A）は機械化困難のため執筆者本人の自己照合＋的絞りWeb確認（§省エネ規律・author-once）。
- **【2026-07-07 完遂】B（🗝記憶のフック）・C（横串trap）は刑法 v13 全 corpus（44本220カード）へ展開完了し、
  起点 `canonical/GENESIS-CARD.html` も完全準拠化済み**。G52/G53/G54 は WARNING→**ERROR 化**したので、以後は
  💭のまま・trap 欠・配線切れの `_lex` を生成・push できない（三層防御＝canonical／validate／push前ゲート）。

### 【2026-07-09 監査＋恒久対策】「完遂」は**構造上**の完遂＝中身の薄さは別ゲート（G56/G57）で拾う

**監査結果**：上記「完遂」は**構造（全カードに非空 trap＋🗝フックを付与）**の完遂であって、**中身の質**の担保ではない。
既存ゲートは `G52＝.tx-v13-trap が非空か`・`G51＝BASIS 存在か`だけを見るので、**「罠ラベルを着た結論言い換え」**や
**「やさしい版でない要点羅列 GIST」**は**素通り**する（§v13m A の GIST 品質は機械化困難と本項が明言）。実地監査で、
薄さは**文書偽造帯 367-385 に集中**（放火 pilot 360-366・ラッパー移行 386-445 は良好〜優秀）と判明した。

**失敗シグネチャ（＝作らせない基準・自己照合の的）**：

| 症状 | ダメな例（薄い） | 直し（やさしい版／横串） |
|---|---|---|
| **① 薄い GIST** | `×。供述調書が証明するのは…156条の「虚偽の文書」に当たらない。`（verdict 直開始・条文羅列・直感の裏切りも用語の生活語開きも無い） | 直感の裏切りで入る→核用語を生活語で開く（用語サンドイッチ）。例＝刑TX375【1】`「悪用する気はなく父に見せて安心させるだけ」なら“使う目的”はない、と言いたくなる。でも…行使の目的（作った文書を本物として役立たせるつもり）は本来の用法に限らない…` |
| **② 薄い罠** | `「押印がない＝無印」と早合点する。署名があれば有印。`（答えの言い換えだけ・別論点との対比が無い） | 似た**別論点**との横串（混同フラグ/対比/反転）を1枠。例＝刑TX375②`外形が同じでも相手方公務員が事情を知っているかで条文が反転する。知らせず欺けば157条（客体限定あり）、通じ合えば65条1項で156条の共同正犯（限定なし）…` |

**恒久対策（三層）**：
1. **作らせない（本項）**：上表の失敗シグネチャを正典化。gold 見本＝**刑TX362／409-425／436-445**。
2. **検出する（`validate-tx-core.py` **G56/G57・WARNING**）**：v13 カードの `.syn-lead`＝`gist_depth_flag()`、
   `.tx-v13-trap`＝`trap_depth_flag()` で薄さの署名を拾う。**ERROR にしない**＝良質だが簡潔な版（ます形やさしい版）を
   誤爆させないため（機械化困難ゆえ助言に留め、著者の自己照合1回を助ける）。単一情報源はこの2関数。
3. **push 前に surfacing（`check-tx-lex-engine.py`・非ブロッキング）**：G56/G57 候補を worklist として表示（push は止めない）。
   corpus 一覧は **`python -X utf8 scripts/check-tx-v13m-depth.py [dir] [--detail]`**（薄い GIST/罠のファイル別枚数）。

**是正状況**：刑TX375 をパイロット是正済み（5 GIST→やさしい版＋薄 trap 2→横串・G56/G57 クリア）。**367-385 帯が worklist**
（`check-tx-v13m-depth.py` の上位）。正誤・判例引用・構造・answer-key は不変のまま中身だけ author-once で厚くする。

## v13n 過去問原文の不可侵ブロック＋別エリア一問一答（2026-07-10・ユーザー合意の正典化）

**背景（実害）：** 事例型 `_lex` で、`#part-a:has(.tx-inline-card) > .problem-text { display:none }`
が**事例（`<p class="problem-text">`）を罪名/記述リストごと非表示**にし、Lexia のインライン周回で
「事例が消える」事故が起きた（刑TX374 で発見）。ユーザー合意の設計＝**別エリア方式**。

**規約：**
1. **不可侵原文ブロック（`.tx-original-block`）**＝過去問PDFの **問い＋事例＋（見解）＋記述/罪名列を逐語のまま**
   常時表示する箱。付け足し・要約・改変・削除ゼロ（区画ラベル「📖過去問原文」「【事例】/【罪名】」だけは
   目印として可）。事例は Lexia が文脈認識する `.case-description > .case-scene > .case-paragraph` 構造で置く
   （`extractCaseContext` が各設問へ文脈添付）。`.problem-text` とは別クラスなので display:none に当たらない。
2. **学習グリッド（別エリア）**＝○×インラインカード／SM2プール。各記述は**事例の前提を含み単独で読めて
   答えられる一問一答**（記号フリー・自己完結）。○×は命題の真偽で固定（G60）。
3. **型による使い分け：**
   - **共有の事例/見解を持つ型**（甲の罪責を…／見解A/B×事例Ⅰ/Ⅱ…）→ 不可侵ブロック**要**。
   - **各記述が独立自己完結の型**（逐語のインラインカードがそのまま原文）→ 不可侵ブロック**不要**
     （箱を作ると長い記述を二重掲載するだけ）。
4. **一問一答の数＝実質の記述数**。表面の選択肢記号数（組合せ①〜⑤・語群(a)〜(j)）ではなく、素材の記述に合わせる。
   組合せは素材の記述、空欄は空欄数。**G62** が「`.ox-row`数＝原文記述数（`.tx-charge`）＝answer-key長」を機械保証。
5. **マーカー字下げ**：丸囲みマーカー（`.tx-charge-mk` 等）は inline-block ゆえ親の `text-indent:1em` を継承して
   円内文字が欠ける。**text-indent:0 を明示**（**G61** が機械検査）。CSS は `canonical/GENESIS-CARD.html` 同梱＝
   複製で自動継承。ブロックが無ければ CSS は不活性。

**恒久ゲート：** `validate-tx-core.py` **G61**（マーカー text-indent:0）・**G62**（記述数一致）。ブロック無しは
スキップ＝既存問誤爆ゼロ。**2026-07-11 に g45 内包から独立ゲートへ昇格し、push 前 `check-tx-lex-engine.py` にも組込**
（従来は g45 の v12.2.1 マーカー判定に内包されており、v13 ファイルでは一切走っていなかった）。
**適用実績（刑法 v13 事例型）：** 刑TX374・刑TX401 完了／刑TX294 は既に良好／
刑TX407・刑TX437 は各記述独立自己完結でブロック不要。旧構造の事例型15問はバグ無し（v13 再生成時に本ブロックを継承）。

**追補（2026-08-02・LEX-439＝再発と検出ゲートの新設）：** ブロックを**作らなかった**共有事例型が corpus に残っており、
刑TX374 と同じ「事例が消える」事故が実機で再発した（実害＝**刑TX439**（【事例】バッグ／実機報告）・**刑TX395**
（【事案】職務質問）・**刑TX218**（【記述】空欄本文＋【語句群】A〜F）。いずれも原文は HTML に在るのに
`#part-a:has(.tx-inline-card) > .problem-text{display:none}` に巻き込まれて非表示）。
根因＝**G61/G62 は「ブロックが有る前提」の検査**（マーカー字下げ・記述数一致）で、ブロック不在の型は
設計どおりスキップされ、誰も見ていなかったこと（規約の記載＝2026-07-11 の穴4は埋めたが、機械検出は不在）。
恒久対策＝`validate-tx-core.py` **G74**（ERROR）＝「インラインカード有り × `.tx-original-block` 無し ×
`#part-a` 直下に**記述の再掲でない長文** `.problem-text`（`.choice-num-inline` 無し・80字以上）」を検出。
記述が独立自己完結の型（全 `.problem-text` が記述再掲）は対象外＝**是正後の全 corpus 642 本で検出 0**（誤爆ゼロ実測）。
push 前 `check-tx-lex-engine.py` にも組込み（三層＝placeholder 契約／validate／push 前）。
上記 3 本は §v13n 正典形（`.tx-original-block` ＞ `.case-description` ＞ `.case-scene` ＞ `.case-paragraph`）へ移設し、
CSS 区画が無い旧世代だったため正典 `GENESIS-CARD.html` の「過去問原文の不可侵ブロック」区画を逐語注入した
（本文・他 CSS は不変）。**記述の二重掲載を避けるため、長い記述はブロックへ入れず問い＋事例だけを載せる**
（刑TX401 の前例どおり。`.tx-charge` を置かない場合 G62 はスキップ）。

## 【2026-07-11 テンプレート監査＋恒久対策】正典 gold 化＋ゲート配線の穴3件を修正

既存問の個別監査ではなく**正典＋ゲート体系のテンプレート監査**で判明した穴と、その恒久対策の記録。

1. **正典見本の薄さ（作らせない層）**：GENESIS-CARD 見本5カードのうち、カード1・3の💡THE GIST が
   verdict＋条文羅列の要点羅列型（やさしい版でない）、カード1の罠が cross-cut 欠落、カード4の罠が
   GIST の言い換え（§v13m 失敗シグネチャ②）だった＝G56/G57 の精度優先設計の盲点内（80字超の平叙羅列と
   「早とちり」「一括把握」語彙は素通り）。→ **4箇所を gold 形へ書き直し**（author-once）。
   カード1罠はリード形①チップの正典見本を兼ねる。**`.tx-cc-tag` チップ CSS を正典同梱**。
2. **push 前ゲートの v13 穴**：`check-tx-lex-engine.py` の G45 適用判定が v12.2.1 マーカーのみ＝v13 ファイルで
   G45（表示LOCK）＋内包の G61/G62 が一切走らず、明示指定でも G45 以外のコードは収集フィルタで捨てられていた。
   → G61/G62 を独立ゲート化（run()＋engine 双方で実行）、G45 判定に LOOP-CARD 追加。**全コーパス実測で
   検出0**（コーパス安全）を確認してから ERROR ゲートとして有効化。
3. **正典自身が検査対象外**：v13 系ゲート（G50-G60 等）の `_lex` stem ガードで `canonical/GENESIS-CARD.html` が
   スキップされ、従来の「GENESIS-CARD 0 error」は v13 ゲート未実行の PASS だった（g45 も GENESIS-CORE のみ対象）。
   → `is_lex_target()`（stem `_lex` または `GENESIS-CARD`）で正典を全 _lex 系ゲートの対象化。
   `validate-staged.py` の ROUTES に `canonical/GENESIS-{CARD,CORE}.html` を追加（正典ステージ時の pre-commit 自動検査）。
   engine のスロット契約検査を CORE＋CARD 両 placeholder へ拡張。pre-commit フックの「既定は report」コメントを
   実装どおり「既定は block」へ訂正。
4. **v13n の生成側配線ゼロ**：spec・placeholder・new-tx.md・new-tx-headless-v13.md のどこにも不可侵ブロックの
   記載が無く、「共有事例型なのに作らない」は G61/G62（ブロック無しスキップ設計）でも無検出＝刑TX374 型の
   事例消失が新規生成で再発しうる状態だった。→ 4ファイルすべてに型判定（共有事例型→必須／独立自己完結型→不要）
   と G61/G62 を明記（spec 第1-bis項・placeholder スロット `{{過去問原文（不可侵）}}`・new-tx 特殊型ガイド・headless Phase 4a〜4g）。

**追補（同日・第2ラウンド＝再生成に依らないシステム改善）**：
5. **G63 三点整合ゲート新設**＝インラインカード⇄SM2プール（.ox-row）⇄answer-key の data-stmt 集合一致・重複なし・
   key長＝row数（G25 WARNING から昇格）。従来この整合は誰も見ておらず、ズレると「プールに出るのに解けない記述」が
   黙って生まれる穴だった。全コーパス実測 検出0 を確認して ERROR 化（run()＋check-tx-lex-engine 双方）。
6. **CSSドリフトの自動可視化**＝`tx-lex-css-canonize.py` の CANON を disk の 刑TX359_lex（正典より古い実体）から
   **正典本体 canonical/GENESIS-CARD.html へ変更**（単一情報源）。engine 末尾で `--check` を**非ブロッキング** surfacing
   （実測 不一致 102/110＝生成世代差。`--apply` 全展開＋実機確認の完了後にブロッキング昇格予定）。
7. 小物＝**G57 語彙拡充**（早とちり・一括把握・一括り・飛びつ＝監査で実証した言い換え罠の盲点語彙）／**G11 解消**
   （正典 tree SVG viewBox 532→552・下端余白 42px）／**🗝フック文言の現実化**（§v13m B＝一行標語＋補足1文まで可）／
   **lineage census 脱数値化**（固定数値の腐り対策＝実測コマンド参照へ）。

## v13p 図解コンポーネント（TX-DGM）＋対比学習の執筆規約（2026-07-13・ユーザー承認の正典化）

学習科学監査（2026-07-13）の帰結：「似て非なる」規範の弁別は**1画面の同時対比**が最も効く。文章に埋まっていた
対比を、固定パターンの純CSS図解（TX-DGM）として物語と記述カードの両方に置く。初例＝**刑TX382_lex**（5枚×両置き）。
**任意項目のため版据置**（is-theory 前例方式・v13.1.0 のまま）。展開方針＝**380-445 を優先改訂、それ以外は TJR 付随**。

**A. 図解本体（構造は GENESIS-CARD.placeholder.html「図解コンポーネント」節が正典）**
- CSS＝canonical/GENESIS-CARD.html 第1 `<style>` の `/* TX-DGM:BEGIN〜END */` 区画。全 v13 共通CSSとして
  `tx-lex-css-canonize.py --apply` で配布済み（2026-07-13・110本）。問題ファイル側は**許可クラスの markup だけ**書く。
- 型＝**対比レーン**（`.tx-dgm-lanes` 2列既定／`.is-3` 3列）と**ステップ＋帰結分岐**（`.tx-dgm-steps`＋`.tx-dgm-fork`）。
  SVG 座標打ち・新規クラス・inline style の自由描画は禁止（G67 ERROR）。
- 置き場所（present なら LOCKED）＝カード：`.syn-path` 直後・🗝記憶のフック直前／物語：該当ラベル段落直後の兄弟 div。
  **論点単位で1枚執筆→物語と該当カードへ同一複製・`data-dgm="{番号}-{連番}"` で同期**（同 id 内容不一致＝G67 ERROR）。
- **効く論点だけ**＝対比・裏命題ペア・分岐・時系列。導入/まとめ・図で冗長になる論点へ強制しない（**偽図解の禁止**）。
  体系マップ縮小版・横断3軸マトリクスの複製は作らない（§v13m D と同じ重複禁止）。
- 帰結チップ＝is-ok 緑（規定・救済・成立が有る側）／is-ng 赤（無い側）／is-teal・is-acc（中立対比）／is-flat。
  図解内の ○/✕ は**法的帰結**の表現で、記述の正誤（判定バッジ・G60 の「命題の真偽」）とは別層。混同させる文言を書かない。
- Lexia 互換（実装確認済み）＝プール抽出は `<p>` のみで図解 div を素通り／v13 reviewFrame（iframe＋style ごと描画）は
  図解をそのまま表示／物語ゼブラ・連番は `> p` 基準で不変。**Lexia 側改修は不要**。
- **レーン見出しの折返し規律（2026-07-29 追補・実機報告＝刑TX434 の見出しが縦1〜2文字ずつに潰れる）**：
  `.dgm-lane-head` は全幅で `flex-wrap:wrap`（640px 以下限定を撤廃）＝見出し（`.dgm-lane-obj`）とピル
  （`.dgm-lane-law`）が同一行に収まらないときはピルが下段へ落ち、見出しをレーン全幅で自然折返しさせる。
  ピルは `white-space:normal`＋`max-width:100%` でレーン幅超過時も枠外へはみ出さない。CSS 変更は正典
  `TX-DGM` 区画＋全既存 513 ファイル（公式・`_lex` 双方）へ決定論置換で伝播済み。

**B. v13m 追補（対比学習の執筆規約・3点）**
- **B-1 フックの対比圧縮型（推奨形）**：対比論点の 🗝記憶のフックは「**AはX、BはY**」型で差分を一文に畳む
  （例：「通貨は『流す』、有価証券は『見せる』で行使になる」）。図解が差分を見せ、フックが畳む、の分業。
- **B-2 BASIS 判例の役割ラベル（任意・推奨）**：表裏ペアがあるとき、判例カード head の括弧で
  **基準を立てる判例／限界を画する判例（裏命題）**の役割を明示する（例＝刑TX379 記述3）。裏命題＝
  「AならばB」の表に対し「AがなければBでもない」を反対側から確認する対の判例・命題。表裏が揃うと
  「Bは A だけで決まる」（基準の必要十分性）まで言え、ひっかけ（効かない要素を効かせる出題）を両方向から遮断できる。
- **B-3 ×記述のプール解説は訂正先頭**：`.ox-pool-gist` は「×。」の直後に**訂正命題（正しくは〜）**を置いてから
  理由に入る（誤情報効果＝誤り文の反復露出が真実感に化けるリスクへの対策。新規生成・更新時から適用・既存一括変換はしない）。

**検証・展開**
- ゲート＝`validate-tx-core.py` **G67**（CSS存在／許可クラスのみ／inline style 禁止／data-dgm 同期）を
  `check-tx-lex-engine.py` の push 前ブロック対象に追加（三層＝placeholder 契約／validate／push 前）。
- 既存展開＝380-445 の v13 本文へ効く論点の図解を優先執筆（カード＋物語の両置き）。379 以下は TJR 付随。
  新規生成は GENESIS-CARD 複製で CSS を自動継承し、執筆時に効く論点だけ図解スロットを埋める。

---

## v13q 答案圧縮（TX-ANSCOMP）＝記述原文統合＋GIST自己完結＋#basis空箱禁止（2026-07-28・ユーザー承認の正典化）

周回の実運用（節の冒頭だけ物語解説で整理し、以後は記述カード解説だけを見る）に合わせ、記述カードを
単体で完結させる 3 点セット。初例＝刑TX425_lex（試作→ユーザー承認）。

**A. 答案圧縮（`.tx-anscomp-line`）＝記述原文ブロック統合・シングルソース**
- 中身＝**正しい規範の完全文**（規範＋条文・判例を答案調 1〜2 文・答案にそのまま書ける形）。強調 `<strong>` は 1〜2 語。
- 置き場所（LOCKED・2 箇所同一文）：①各記述カード `.syn-orig`（📜記述原文）の**末尾**に
  `<span class="tx-anscomp-line">…</span>`（`</p>` 直前）。②正誤表 `tr` の **`data-brief-mark` 属性値の末尾**に
  `<span class='tx-anscomp-line'>…</span>`（属性内は単引用符）。両者は**シングルソース**＝タグ除去後の
  正規化テキストが一致（カードと正誤表のどちらを見ても同じ規範文が目に入る）。
- **訂正チップとの分業**：×記述の `.tx-stmt-fix` は**最小訂正**（「→ 要しない（一般的職務権限に属すれば足りる）」等）に
  留め、完全規範文は答案圧縮へ一本化する（長文チップとの二重掲載＝試作前の 425 型重複を禁止）。
- 意匠＝**シルバー食み出しタブ＋小箱**（他ラベルと同じ「上端タブ・本文は下」形式。inline チップに文をぶら下げない）。
  CSS は `canonical/GENESIS-CARD.html` 第1 `<style>` の `/* TX-ANSCOMP:BEGIN〜END */` 区画（複製で自動継承）。
- 体系マップ札の ✍ 一行との関係＝札は短縮形・答案圧縮は完全文（同じ規範の二段階表示・向きを揃える）。
- 判定バッジは「✗ ×（誤り）」等の短形のまま（一言理由は直下の答案圧縮が担うため二重化しない）。

**B. THE GIST の自己完結（§v13m A の増補）**
- 構成＝**結論先出し（○/×）→ 体系的位置づけ → 専門用語（用語サンドイッチで開く）→ わかりやすい例えでイメージ →
  本記述の当否**。物語解説を読み返さなくてもカード単体で理解が立ち上がる密度にする（物語＝初回整理／GIST＝周回時の本文）。
- 例えは当該記述の🗝記憶のフックの標語と**同じイメージ系**で揃える（GIST で像をつくり、フックの一行で固定する二段構え。
  刑TX425＝引き出し／名刺／受け皿／制服／値札）。用語サンドイッチ・核用語太字 1〜2 語・用語スープ禁止は §v13m A のまま。

**C. #basis（共通根拠・最新法令ノート）の空箱禁止**
- `tx-current-law-note` を使う問題だけ `#basis` セクションに中身を書く。**使わない問題は
  `<section class="section" id="basis" hidden=""></section>` の不可視アンカー**に落とす
  （G4 の存在チェックは満たしつつ、中身が「↑ページ先頭へ」だけの白箱を出さない）。

**検証・展開**
- ゲート＝`validate-tx-core.py` **G73**（片置き＝カード⇄正誤表の一方にだけ有る／文の不一致／CSS区画欠落＝ERROR。
  未展開（全記述に無し）と可視の空 #basis 箱は WARNING 助言）を `check-tx-lex-engine.py` の push 前ブロック対象に
  追加（三層＝placeholder 契約で作らせない／validate で弾く／push 前に止める）。
- 既存展開＝**TJR 付随で消化**（答案圧縮・GIST 増補は内容執筆を要するため決定論ツール化しない。CSS は
  GENESIS-CARD 複製／再生成で自動継承）。新規生成＝new-tx／headless プロンプトに執筆工程を組込み済み。
- 執筆者自己照合（author-once）に 1 項目追加＝「答案圧縮はカードと正誤表で同一文か・○×の向き（G60 の命題の真偽）と
  整合するか」。

## v13s 設問・問題文の原文は全ファイルに載せる（2026-08-05・LEX-005・ユーザー指示の正典化）

**実機報告（刑訴TX005_lex・おとり捜査）**：周回画面に設問リードだけが出て、【会話】①〜⑥も
【記述】ア〜クも表示されない＝**問題そのものが読めない**。公式 `刑訴TX005.html` には
【会話】【記述】【組合せ】が全て入っており、`_lex` へ組み替える工程で共有本体だけが落ちていた。
ユーザー指示は「**試験問題に関する設問及び問題文の原文は、すべてのファイルに乗せて欲しい**」。

### 何が構造的な穴だったか

- `_lex` の記述カードは**各記述を書き換えた命題**であって問題本体ではない。会話・事例・語句群・
  図表のような**共有本体はカードに複製されない**ので、移送し忘れると復元不能な欠落になる。
- §v13n が「共有事例型のみ」と読め、移送するか否かが生成者の判断に委ねられていた。
- **G74 は「DOM に在るが `display:none` で消える」型専用**（`#part-a` 直下の長文 `.problem-text` を見る）。
  「そもそも作らなかった」型は DOM に何も無いので設計どおり素通りする。つまり **`_lex` 単体では
  検査不能**で、公式（唯一の原文ソース）と対照しない限り機械検出できなかった。

### 規約

1. **公式 `000_TX` が持つ設問リードと問題本体（【会話】【事例】【語句群】【記述】【組合せ】）は、
   `_lex` にも必ず載せる。** 共有事例型に限らない。
2. 置き場所は §v13n の `.tx-original-block`。`#part-a:has(.tx-inline-card) > .problem-text{display:none}`
   に当たらない唯一の常時表示エリアなので、ここ以外へ置くと周回中に消える（刑TX374 型）。
3. **原文以外は入れない**：参照条文（`#basis` の領分）・解答・編集注記（「📖 本問…の本物の5択」）。
   正解の先出し（「正解：肢3」「／正答率89%」）は §v13r・G19 の禁止事項でもある。
4. 記述が独立自己完結の型（インラインカードがそのまま原文）でも、**設問リードの原文層**
   （`.tx-original-lead`）は置く。原文と操作指示は二層に分ける（§v13r・初例＝刑訴TX002_lex）。

### 恒久ゲート（三層）

| 層 | 実装 | 効き方 |
|---|---|---|
| 作らせない | `canonical/GENESIS-CARD.placeholder.html` §v13s 契約 | 生成時に移送を必須化 |
| 弾く | `validate-tx-core.py` **G75** | 公式と対照。**公式から逐語復元できる型＝ERROR**／原文層なし・言い換え版あり＝WARNING |
| 止める | push 前 `check-tx-lex-engine.py` に G75 組込み | ERROR は push 停止 |

判定式の**単一情報源＝`scripts/tx_source_text.py`**。検出ゲート（G75）と復元ツール
（`scripts/tx-restore-original.py`）が同じ関数を使うので、**検出 ⟺ 修復可能**が同値になり
「直したのに ERROR が消えない」「ERROR は出ないのに欠けている」が構造的に起きない。

### 既存展開

- **決定論的復元**＝`python scripts/tx-restore-original.py --apply`（冪等・CRLF 保存・本文不変・
  公式を単一情報源に逐語移送）で **13 ファイル**を 2026-08-05 復元済み。restorable 残 0。
- **原文層を持たない `_lex` 543 本（うちインライン周回 295 本）**は入力 PDF を見て 1 問ずつ復元する
  （原文が公式にも無いため決定論ツール化できない）＝**TJR 付随で消化**。
  残件一覧＝`docs/tx-source-text-audit.md`（再生成＝`python scripts/tx-restore-original.py --report`）。
- 執筆者自己照合（author-once）に 1 項目追加＝「この `_lex` だけを見て、原文の設問と問題本体
  （会話・事例・語句群・記述・組合せ）が読めるか」。

---

## v13t 正誤表コア列の実体名ラベル化＋残り法理の details（2026-08-08・ユーザー運用の正典化）

### きっかけ（実運用）

ユーザーの周回運用が「**問題を解く → 正誤表を見る → 取れた肢は 原文→✍答案圧縮 を読んで次へ、
外した肢だけ本解説へジャンプして熟読**」に落ち着いた。狙いは**正誤表だけで周回を完結させ、
ジャンプ回数を減らすこと**。この運用に対して、旧実装は次の 2 点でズレていた。

### 何が構造的な穴だったか

1. **「コア」という名で出ていたのは実は「転用」行だった。** `extractReviewCoreSummary` の抽出優先順位が
   転用 → 射程 → 切断点 → 文言 → 先頭 で、corpus 実測では **5タグが 2,334 行すべてに揃っている**ため
   事実上 100% 転用行が出ていた。ラベルが実体と違うので、読み手も執筆者も何を書けばよいか分からない。
2. **文言・趣旨・射程・切断点の 4 行は、インライン周回で一度も露出しない。** 元表は
   `.answer-area.inline-prototype-mode .final-answer{display:none}` で隠れ、露出面はクローンパネルの
   コア列 1 行だけ。**インライン 464 本 × 2,334 行 × 4 行 ≒ 9,300 行**の執筆資産が周回では死蔵していた
   （Lexia 復習プールが読むのは `.ox-pool-explain` なので、そちらでも使われていない）。
3. 結果、**外した肢の「どこで正誤が分かれたか＝切断点」が表に出ない**ので、毎回カードへ飛ぶしかなかった。

### 規約（v2＝TX-VERDICT-CORE2）

- コア列は**実体名ラベル**で出す（「コア」という総称ラベルは廃止）。
  - **外した行**（`.tx-user-incorrect`）＝ `切断点`（`.tx-vb-cut`）＋ `転用`（`.tx-vb-carry`）の 2 行。
  - **取れた行**（`.tx-user-correct`）＝ `転用` 1 行（原文→答案圧縮→転用 で次へ進める最短動線）。
  - **未回答／閲覧のみ**＝記述の正誤で振る（×＝切断点＋転用／○＝転用）。
- 表に出さない残り（文言・趣旨・射程ほか）は **既定閉じの `<details class="tx-vb-rest">`**（「＋ 文言・趣旨・射程 を開く」）
  に畳み、**表内で開ける**。周回速度は落とさず、書いた法理を死蔵させない。
- 見出し列は「各記述の原文（誤り箇所に下線＋正解）と**切断点・転用**」。
- 5タグ（文言・趣旨・射程・切断点・転用）を書く執筆規約は**不変**。変わったのは露出面だけで、
  `.tx-reflex-core` は従来どおり元表に DOM 保持する（answer-key 源・G33）。
- **執筆側の含意**：`切断点` は「外した人がその場で腑に落ちる 1 文」、`転用` は「次の問題へ持ち出す 1 文」
  として書き分ける（旧来は転用行だけが露出面だったため、転用に切断点の内容を混ぜた行が散見される）。

### 恒久ゲート（三層）

| 層 | 実装 | 効き方 |
|---|---|---|
| 作らせない | `canonical/GENESIS-CARD.html`／`GENESIS-CARD.placeholder.html` §v13t 契約 | 複製で v2 エンジン・CSS を自動継承 |
| 弾く | `validate-tx-core.py` **G76** | インライン周回 `_lex` に v2 エンジン（`extractReviewCoreLines`/`appendCoreLines`）＋CSS 区画（`TX-VERDICT-CORE2`）が無い＝ERROR |
| 止める | push 前 `check-tx-lex-engine.py` に G76 組込み | ERROR は push 停止 |

G45（v12.2.1 表示LOCK）の必須シグネチャも `extractReviewCoreSummary` → `extractReviewCoreLines` へ更新した。

### 既存展開

- **決定論的伝播＝`python scripts/tx-lex-verdict-redesign.py <files>`**（冪等・本文不変・`--ref` で正典追従）で
  **インライン周回 464 本**へ 2026-08-08 展開済み。同ツールの改行保存を修正（**LF/CRLF 混在ファイルを
  全体正規化して全行差分を出していた**バグ＝差し込み箇所だけ局所様式に合わせる方式へ）。
- 静的（インラインカードなし）の `_lex` 248 本は元表がそのまま全 5 行を表示するため対象外。

### 追補（2026-08-08 同日・実機フィードバック）：ラベルのぶら下がり解消＋誌面の作り込み

実機で 2 点の指摘。どちらも**正誤表全体**の見え方の問題で、CSS 区画 `TX-VERDICT-CORE2` に統合した。

1. **ラベルに本文がぶら下がる。** `.tx-vb-line` はラベル（`inline-block`）＋テキストノードの素の
   インライン流しだったため、**折返し 2 行目以降がラベルの真下へ回り込んで**いた（原文・切断点・転用の
   すべて）。→ **ラベル＝固定幅チップ（`min-width:70px`・中央寄せ）＋本文＝ぶら下げインデント**
   （`padding-left:78px` / `text-indent:-78px`）の 2 カラムにそろえた。元表 `.tx-reflex-line`（5.4em の
   ぶら下げ）・AXIOM の `refs` 2 カラムと同型。パネル内 `p` の `text-indent:0 !important` を後段で
   打ち消すため、CSS 区画は**第1 `<style>` の末尾**に置く（順序に依存する＝区画を移動しない）。
   `.tx-anscomp-line` は本文列に入るので `text-indent:1em` を明示で戻す。
2. **フォントが質素。** 表全体が既定ゴシック（`--font-body`）のままで、役割フォントで作り込まれた
   解説カードに対して誌面が痩せて見えた。→ 役割フォントを配当：**ヘッダー・記述番号＝`--font-display`**
   （Shippori Mincho B1）／**○×＝`--font-impact`**（M PLUS 1p）／**印付き記述原文＝`--ed-quote`**
   （Noto Serif JP・各肢カード `.syn-orig` と同じ引用明朝）／**法理（切断点・転用）＝`--font-judgment`**
   （Zen Old Mincho）／**ラベル＝`--font-impact`**／**残り法理＝`--font-note`**。セル余白も 13/14px へ。
   使うのは HEAD で読み込み済みの 12 ファミリーだけ（未読込フォントを増やさない＝G68 の趣旨）。

伝播ツールも**同期型**へ変更した：`TX-VERDICT-CORE2` 区画が既にあっても**正典と差があれば載せ替える**
（旧＝マーカーが在れば skip。それだと正典追補のたびに手作業の再注入が要り、接ぎ木の温床になる）。
エンジンも同様に差分同期。冪等（差が無ければ「変更なし」）。

### 追補2（2026-08-08 同日・実機フィードバック2）：✓/✕ マークのずれ・答案圧縮のにじみ・1字下げ

実機（Windows・Chrome・webfont 読込あり）で 3 点。①が追補1 の副作用、②③は既存の作法漏れ。

1. **印付き原文の ✓/✕ が本文から約 6 文字ぶん左へずれ、直前の文字に重なる。** 追補1 の
   ぶら下げインデント（`text-indent:-78px`）は**継承する**プロパティで、`.tx-stmt-mk` のような
   **inline-block の子は自分の内容にも -78px を適用する**（インライン要素と違い、自前の行ボックスを
   持つため）。→ `.tx-vb-line *{ text-indent:0 }` で子孫を戻し、`.tx-anscomp-line` だけ `1em` を復帰。
   **教訓：ぶら下げインデントを親に敷いたら、inline-block の子孫は必ず `text-indent:0` で戻す。**
2. **✍答案圧縮のフォントがにじむ。** `--font-answer` の実体 **Shippori Antique は 400 の 1 ウェイト
   しか読み込んでいない**のに `font-weight:600`（本文）・`800`（`<strong>`）を当てていたため、
   ブラウザが**合成ボールド（faux bold）**を描いていた。→ 本文は 400（色 `#343b45` で締める）、
   強調は実ウェイトを持つ `--font-display`（Shippori Mincho B1 700）へ。**未読込ウェイトを要求しない**
   （G68 が「未定義フォント変数」を弾くのと同じ発想の、ウェイト版の作法）。
3. **ラベルの後に 1 字ぶんの空きが無い。** 正典の作法（v12.2.1 の `.tx-mini-law-body`・
   `.tx-op-body`＝2カラム＋本文先頭1字下げ）に合わせ、ラベルの `margin-right` を 8px→24px
   （≒ 8px＋1字）にして、**折返し行は列頭に揃え、1 行目だけ 1 字下がる**形にした。

`.tx-anscomp-line` は正誤表とカードの両方に出るため、伝播ツールの同期対象に **TX-ANSCOMP 区画**を追加した
（表だけ直ってカードが取り残されるのを防ぐ）。同期は BEGIN/END マーカー単位・冪等。

### 追補3（2026-08-08 同日）：引き出し（文言・趣旨・射程・切断点）の可読性

開いた 4 点が同じ灰色ラベルで並ぶだけだと、どこからどこまでが 1 役割か目で追えない。次の 3 点にした。

- **1 枚の「引き出し面」にまとめる**（`.tx-vb-rest-body`＝淡い羊皮紙地＋左レール＋角丸）。表の地と別の層に
  見えるので、閉じている時／開いた時の切り替わりが分かり、本文（原文・答案圧縮・切断点・転用）と混ざらない。
- **1 行＝1 役割で点線区切り**（最終行は区切りなし）。行間ではなく罫で切ることで、長い行でも役割の境界が分かる。
- **役割色（淡地＋濃文字）**：文言＝条文のブルー／趣旨＝理由のグリーン／射程＝範囲のバイオレット／
  切断点＝分かれ目のテラコッタ（主行の切断点と同系）。主行のラベルは**濃地＋白文字**なので、
  引き出しは一段弱い階層として読める（pale bg + dark text の原則も守る）。
- **読む順を 文言 → 趣旨 → 射程 → 切断点 に固定**（エンジンが並べ替える）。DOM 順の揺れを吸収し、
  「条文の言葉 → なぜ → どこまで → 分かれ目」の思考順で読める。

---

## §v13u BASIS の役割フォント割当て（2026-08-17・LEX-441・ユーザー指摘）

実機（刑訴 `_lex`）で「フォント種類の割り当てがルールに沿っていない」との指摘。監査したところ、
**フォント12変数（LEX-388/G68）・Google Fonts リンク・font-family を持つ CSS 232 セレクタは
刑訴 334 本すべて `canonical/GENESIS-CARD.html` とバイト一致**で、逸脱は CSS ではなく
**📚BASIS のカード種別と本文クラスの対応**（＝どの役割フォントが当たるか）に出ていた。

### 規約（v13 LOOP-CARD）

| BASIS アイテム種別 | 本文の役割クラス | 実効フォント |
|---|---|---|
| 判例 `.tx-basis-item.is-case` | `judgment-text`（`p` 直付け／`p.hanging` なら `.hang-body` に併記） | `--font-judgment`（Zen Old Mincho 700） |
| 条文（種別なし） | 役割クラス無し（`p.hanging`＋`.para-num`／`.hang-body`） | 親 `.tx-inline-explain` の `--font-answer` を継承 |
| 学説 `.tx-basis-item.is-theory` | 役割クラス無し（`p.hanging` または素の `p`） | 同上 |

`judgment-text` は**判旨専用**。学説・条文の本文に付けない。逆に判旨へ付け忘れると、
判旨だけ答案書体で流れて「判例の引用」に見えなくなる。

### 実害（2026-08-17 監査）

| 型 | 症状 | 刑訴の該当（`_lex`／公式ミラー） |
|---|---|---|
| A | 判例カードの判旨に `judgment-text` が無い＝判旨が答案書体 | 刑訴TX006(14)／TX033(3)／TX016(1)＝**18 件** |
| B | 中身は判旨なのにアイテムが種別なし＝**枠・見出しがブルー（条文）で出る** | 刑訴TX038(2)／TX045(1)／TX079(3)／TX126(2)／TX269(3)／TX289(5)＝**16 件** |
| C | 学説カードに `judgment-text`＝学説が判旨書体 | 刑訴TX111(5)／TX252(8)／TX253(1)／TX254(1)＝**15 件** |

正例は **刑訴TX302_lex**（`<p class="hanging"><span class="para-num">判旨</span>
<span class="hang-body judgment-text">…`）。A 型はこの形へ揃える。
`theory` の素の `<p>`（クラス無し）は `p.hanging` と実効フォントが同じ（どちらも継承）ため逸脱ではない。

### 恒久ゲート三層

| 層 | 実装 | 効果 |
|---|---|---|
| 作らせない | 種別と本文クラスの対応表（本節） | 生成時に迷わない |
| 弾く | `validate-tx-core.py` **G77**（判定＝`scripts/tx_basis_roles.py` 単一情報源） | 種別⇄役割フォントの不一致を検出（当面 WARNING） |
| 止める | push 前 `check-tx-lex-engine.py` の G77 助言 | 非ブロッキング（刑法22本・民法1本の同型が残るため。全科目是正後に ERROR 昇格） |

**既存一括是正＝`python -X utf8 scripts/tx-basis-role-fix.py <files>`**（決定論・冪等・本文不変・
CRLF 保存・`--check` で検出のみ）。刑訴 668 ファイルへ 2026-08-17 適用済み（86 件・23 ファイル・
本文テキスト差分 0）。**刑法22本71件・民法1本1件は未修正＝TJR 付随で消化**（ユーザー指示：今回は刑訴のみ）。

### 検出穴の是正（2026-08-19・LEX-442・ユーザー再指摘「刑訴 _lex でフォント割当てが正典どおりでない」）

再監査したところ、**CSS は 刑訴 334 本すべて canonical とバイト一致のまま**（font-family を持つ 254 規則が
全一致）で、残っていたのは G77 の**判定式が構造的に見落とす 2 型**だった。単一情報源
`scripts/tx_basis_roles.py` を是正し、`tx-basis-role-fix.py`／G77 の双方に同時に効かせた。

| 穴 | 何を見落としていたか | 実測 | 実害 |
|---|---|---|---|
| 1 | 旧 `P_RE` が `<p class="…">` の直後が `>` の形しか拾わず、`<p class="judgment-text" data-hyakusen="…">` のように**属性が続く段落を全て素通り** | honbun の `<p>` 13,182 中 **67 段落／27 ファイル** | **刑訴TX126**（判旨 2 枚が種別なし＝条文枠のブルーで出るのに G77 は 0 件） |
| 2 | **判旨なのに `judgment-text` も `is-case` も無い二重欠落**（型 D）。A（判例カード側の欠落）にも B（`judgment-text` からの逆算）にも掛からず設計上検出不能 | **24 件／10 ファイル** | **刑訴TX324**（判旨 5 枚が条文枠＋答案書体） |

判定の追加規約：`<span class="para-num">判旨</span>` の段落は判旨とみなし `judgment-text` を要求する（型 D）。
**段落が全て判旨のカードだけ** `is-case` へ昇格させる（型 B）＝条文本文と判旨が同居する複合カード
（例 刑訴TX324「📜 刑訴305条／最大決昭33.2.26」）は種別を変えず、判旨段落にだけ判旨フォントを当てる。

**刑訴は 2026-08-19 に再是正済み**（刑訴TX126＝B 2 件／刑訴TX324＝D 5 件＋B 4 件・`_lex` と公式ミラー
計 4 ファイル 22 件・**本文テキスト差分 0**・再走査 668 ファイルで残 0）。**刑法 136 件・民法 2 件は
TJR 付随で消化**（旧検出式での 108／2 から、穴 1・2 の是正で 刑法 +28 件が可視化された）。

> なお「フォントが 1 種類に見える」体感の主因は**この型の逸脱ではなく下記「層1」**である。
> 例えば 刑訴051〜064 は論点が勾留の条文知識に寄っていて**判例カードが 1 枚も無い**帯であり、
> 条文本文には `--font-statute` が当たらない（`--font-answer` を継承する）ため、
> 誌面全体が Shippori Antique 一色になる（実機の computed style で確認）。逸脱ではなく仕様どおりの帰結。
### 併せて確認した「層1」（正典そのものの役割割当て・未修正）

12役割マトリクス（`spec/jx-v3.2-master.md` §6-4）と TX v13 実装のズレ。**今回は変更していない**
（誌面が全問変わるため、着手時は実機確認が必要）。記録として残す：

- ④ 条文 `--font-statute`（Noto Serif JP）… 条文**本文**には当たっておらず（`--font-answer` 継承／
  静的版は `.basis-card-body` の `--font-quote`）、`.para-num`・`.ron-mark`・強調マーカーのみ。
- ① 本文 `--font-body`（A1 Gothic）… `body` は後段で `var(--ed-sans)` に上書き。生きているのは SVG ノード 5 規則のみ。
- ⑦ KEY `--font-keyword`（Kaisei Decol）… 参照 0 件＝役割が未使用。
- ⑩ 教授 `--font-professor` … CSS 2 規則あるが v13 カードに実体なし。
