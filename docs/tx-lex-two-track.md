# TX 二系統化（公式 / Lexia用）と出題タイプ別テンプレート — 全展開ポリシー

> 確定日 2026-06-24（パイロット刑TX001/010/055/056/057/350 で実証済み）。
> 短答 TX を「**公式（過去問5択）**」と「**Lexia用（学習・復習プール）**」の2系統で持つ。
> 全展開（既存362問＋新規）はローカルPC のパイプラインで本ポリシーに従って行う。

---

## 0. 2系統の役割と保存先・命名

| 系統 | 保存先 | 命名 | 中身 | Lexia 取込 |
|---|---|---|---|---|
| **公式** | `outputs/000_TX/{科目}/` | `刑TXNNN.html` | 過去問形式の**単一5択**（組合せ番号 等） | ✕ 入れない |
| **Lexia用** | `outputs/ux/000_TX/{科目}/` | `刑TXNNN_lex.html` | ox-grid の一問一答＋解法ナビ＋自己完結命題 | ◯ これだけ |

- Lexia は `src/lib/barExamSync.js` の `filterLexiaImportable` で **`ux/000_TX` の `_lex` を取込み、対応する公式 `000_TX` は除外**（`_lex` 未生成の問題は移行期のみ従来 `000_TX` を取込）。
- `_lex` は `extractCode` で公式と同一コード（例 `TX350`）に正規化＝Lexia 上で増殖しない。

---

## 1. 出題タイプ別の出し分け（最重要）

PART A の出題形式を判定し、テンプレートを出し分ける。刑法362問の内訳（ヒューリスティック・全展開時は1問ずつ最終確認）：

| タイプ | 件数目安 | Lexia用テンプレ | 参照（gold） |
|---|---|---|---|
| **① 記号のみ組合せ**（`①b②c…`が肢） | 6（114/119/174/218/344/351） | **350方式**（最優先・現状プール価値ゼロ） | `刑TX350_lex.html` |
| **② 語句組合せ型**（空欄①〜⑤に語句） | 約61（010 等） | **350方式** | `刑TX350_lex.html` |
| **③ 見解対比型**（PART Aに【見解】A説/B説） | 36（057 等） | **○×＋記号→実体置換**（350構造は任意） | `刑TX057_lex.html` |
| **④ 通常○×**（独立5記述の正誤） | 259 | **素の○×ox-grid＋軽量ナビ** | `刑TX001_lex.html` |

- ①②＝**約67問が350方式の本命**。③は構造は○×のままでよいが肢の記号参照を実体化。④は現状維持。

---

## 2. 3レイヤ分離（肢の中身の思想）

| レイヤ | 置き場所 | 記号 |
|---|---|---|
| **過去問原文** | PART A／問題文／【組合せ】 | 記号OK（過去問忠実性） |
| **学習プール**（`.ox-stmt`＝Lexia 一問一答／verdict「論点のコア」列） | ox-grid | **記号フリーの自己完結命題**（条文・判例つき・転用可能な法理）。`＝b`/`アの見解からは`等は実体（不限定説／結果無価値論）に置換 |
| **解法テクニック**（消去・逆算 等） | 解法ナビ／PART B 解説 | 知識カードに混ぜない |

---

## 3. 350方式（組合せ型 Lexia用）の構造

各空欄①〜⑤＝1 ox-row。3要素：
1. **設問** `.ox-q`（解く）
2. **過去問選択肢** `.ox-btn`（`data-value`=letter・表示のみ・記号OK）
3. **自己完結命題** `.ox-stmt`（`.ox-core-wrap` 内・reveal 後に展開・**プールに入るのはこれだけ**）

- `data-correct-value` は letter 連結（例 `bcfgj`）。**既存採点エンジン（handleOxBtn/handleRevealAnswerBtn・value非依存）がそのまま採点**。
- verdict 表は「空欄｜正解｜論点のコア」。`data-answer-key="1:b,2:c,3:f,4:g,5:j"`（letter）で G23 充足。
- **解法ナビ**＝回答ボタンを持たず、検討順（ORDER）＋💡コツ＋組合せ消去パネルでガイド。ユーザーは下の ox-grid で逐一回答→候補が絞られる→最後に reveal。
- 配色：正解＝**淡緑地＋濃緑字**（design system）。コツ・論点のコアは**ラベル見出し＋本文を次行で字下げ（1.5em ハンギング）**。

## 3-bis. ○×方式（通常型 Lexia用・001 基準）

- ox-grid は各記述 ○/×（既に「選択肢＝○×」で自己完結）。
- 解法ナビは○×型エンジン（記述を順に判定→「正しい/誤っているものはどれか」の候補から消去→残り番号→確定で採点）。
- mode は `data-correct-value` の ○/× 個数で自動判定（○1個=正しいものはどれか／×1個=誤っているものはどれか）。

---

## 4. 検証ゲート

- `scripts/validate-tx-core.py`
  - **G29**：正解文字が「その行の選択肢(`data-value`)のどれか」（○×／letter 両対応・半角混入検出）。
  - **G30**：`.ox-stmt` が記号のみ（丸数字3つ以上 かつ 実質日本語2文字以下）でないこと＝Lexia 復習プールの記号肢を生成時に禁止（Lexia `isSymbolOnlyStmt`／commit 2ebbe70 の引き継ぎ）。
- `scripts/check-duplicates.py`：公式↔`_lex` は意図的ミラーとして DUP-TITLE 免除（`_mirror_sig` が末尾 `_lex` を正規化）。
- 公式（単一5択）は v11 ox-grid 前提の validate-tx-core 対象外（check-duplicates のみ）。

---

## 5. 全展開の手順（ローカルPC）

1. 各問の PART A 出題形式を判定（§1 の4分類）。
2. 公式 `000_TX/刑TXNNN.html`＝過去問の単一5択に（既存 ox-grid から or PDF から）。
3. Lexia用 `ux/000_TX/刑TXNNN_lex.html` を type 別テンプレで生成：
   - ①②→350方式（`刑TX350_lex.html` を基準に空欄→設問＋選択肢＋命題）。
   - ③→○×＋記号→実体置換（`刑TX057_lex.html` 基準）。
   - ④→素の○×＋軽量ナビ（`刑TX001_lex.html` 基準）。
4. `validate-tx-core.py`（_lex）G1〜G30 PASS／`check-duplicates.py outputs` PASS を確認。
5. commit/push（pre-commit フックで `lexia-genmeta` スタンプ）。
6. **記号のみ6問（114/119/174/218/344/351）を最優先**で350化（現状プール価値ゼロ）。

- 参照スクリプト：`scripts/lex/build_pilot.py`（○×型・複数問一括の雛形）。組合せ型(350)の変換手順は本書 §3。
- ナビ文面（💡コツ・論点のコア命題）は問題ごとに執筆が必要（量が多い＝複数セッション／バッチ）。

---

## 6. 参照ファイル（gold 基準）

- 組合せ型 Lexia用：`outputs/ux/000_TX/001_刑法/刑TX350_lex.html`
- ○×型 Lexia用：`outputs/ux/000_TX/001_刑法/刑TX001_lex.html`（誤り選択型は `刑TX057_lex.html`）
- 公式（単一5択）：`outputs/000_TX/001_刑法/刑TX350.html`・`刑TX001.html`
