# TX v13 移行対象（旧レイアウト→v13.1.0 LOOP-CARD・刑法）

> 旧レイアウト `_lex`（`getInlineAnswerTablePanel` 無し）でPDFが `inputs/000_TX/001_刑法/` にある問題。
> ローカルバッチ（`TX-PICK -SpecVersion v13`）の対象。gold見本＝刑TX125（2026-07-04 パイロット確定）。
> 生成日: (自動算出) / 対象 190 問 / 番号帯 55〜330。PDFがDriveのみの旧問は別途。

---

## 特殊型（非単純5択）の v13.1.0 化・標準手順（2026-07-07 確定）

**特殊型＝≠5記述・組合せ・穴埋め・見解A/B・事例Ⅰ/Ⅱ。** 決定論ラッパー `tx-lex-v11-to-v13.py` は
5記述・客体三分モデル固定なので特殊型は非対応（掛けると `SpecialTypeError` で exit 3・R 再生成へ誘導）。
**特殊型は「PDF から R 再生成」が唯一の正しい経路**（LLM 生成＝型に非依存。`new-tx.md` が組合せ/穴埋め/見解/
事例を明示的に扱い、検証ゲート G50〜G54 は記述数を数えず N に不依存）。

1. **backlog を機械監査**：`python -X utf8 scripts/tx-classify-type.py` で特殊型かつ非最新版を一覧化。
   `--regenerable` で「ローカル PDF あり＝いま R 再生成可能」だけに絞る。`--json` で機械可読。
   （2026-07-07 時点：特殊型 backlog 128 本／PDFあり 80・Driveのみ 48。Driveのみは PDF 回収後に対象化。）
2. **R 再生成**：`pwsh scripts/tx-v13-runner.ps1 -Subject 刑 -Regen -FromNumber N -ToNumber N`
   （または「TJR処理 刑（Rだけ）」）。PDF から公式＋`_lex` を最新 v13.1.0 で作り直す。記述数 N は5に潰さない。
3. **構造の写経元（型別合格実例）**：穴埋め=刑TX368（v13.1.0・nb-badge 完備）／組合せ・N≠5=刑TX089・174・218・256
   （COMBOエンジン）／見解=刑TX290。`new-tx.md` の「特殊型の構造・作図ガイド」に明記済み。
4. **検証**：`validate-tx-core.py`（_lex/公式）＋`check-tx-lex-engine.py`＋`check-duplicates.py` を通す。
5. **注意（版判定）**：`-Regen` は v13 世代（v13.0.0/v13.1.0）を SKIP し、旧版（v11.x/タグ無し）だけを対象にする。
   v13.0.0→v13.1.0 の底上げは PDF 再生成ではなく内容保存の verdict-redesign 経路（下記「26本」）で行う。

初例＝刑TX381（8穴埋め学説型・上記 361-400 監査所見参照）。

## 連続レンジ（バッチ分割の目安）
- 55（1問）
- 65〜84（20問）
- 126〜173（48問）
- 175〜217（43問）
- 219〜255（37問）
- 257〜289（33問）
- 303（1問）
- 324〜330（7問）

## 全番号
55 65 66 67 68 69 70 71 72 73 74 75 76 77 78 79 80 81 82 83 84 126 127 128 129 130 131 132 133 134 135 136 137 138 139 140 141 142 143 144 145 146 147 148 149 150 151 152 153 154 155 156 157 158 159 160 161 162 163 164 165 166 167 168 169 170 171 172 173 175 176 177 178 179 180 181 182 183 184 185 186 187 188 189 190 191 192 193 194 195 196 197 198 199 200 201 202 203 204 205 206 207 208 209 210 211 212 213 214 215 216 217 219 220 221 222 223 224 225 226 227 228 229 230 231 232 233 234 235 236 237 238 239 240 241 242 243 244 245 246 247 248 249 250 251 252 253 254 255 257 258 259 260 261 262 263 264 265 266 267 268 269 270 271 272 273 274 275 276 277 278 279 280 281 282 283 284 285 286 287 288 289 303 324 325 326 327 328 329 330

---

## 361-400 監査所見（2026-07-07・G51/G52/G53 導入時に確定）

361-400 は v13.1.0 生成済み帯だが、内容の合意（v13m 横串 trap・BASIS 中身・相互リンク）に取り残しがあった。
機械ゲート G51/G52/G53 で確定した残務台帳：

- **刑TX368**：BASIS 全5空箱＋横串 trap 全欠 → **修復済み**（159条1項＋最決平5.10.5＋横串 trap 鋳造・G51 解消）。
- **刑TX381（v13.1.0 化済み・2026-07-07 パイロットで解消）**：**8穴埋めの学生対話・学説型**（写真コピーの
  文書性／補助公務員の作成権限／有形・無形偽造／間接正犯）。決定論ラッパー `tx-lex-v11-to-v13.py`（5記述固定）
  とは構造的に非互換だが、**PDF がローカル回収済み**になったため **Rストリーム（`tx-v13-runner.ps1 -Regen`＝
  PDF→v13）で再生成**して解消。記述数 N=8 を5に潰さず **8カード／8 ox-row／#stmt-1..8** で維持し、COMBOエンジン・
  nb-badge・横串trap を実装。validate-tx-core（_lex/公式/engine）0 error 0 warning・check-tx-lex-engine PASS・
  check-duplicates クリーン。**教訓＝特殊型は「ラッパーで自動変換」ではなく「PDFから R 再生成」が正しい経路**。
  ラッパーは特殊型を検出したら exit 3 で R 再生成へ誘導する（`SpecialTypeError`）。
- **横串 trap 欠落 18本・計55カード（G52 WARNING）**：362, 366, 367, 369-380, 382-385。各カードに
  横串・誤解の罠が未執筆。**執筆時に v13m 規約（§v13m C）で埋める**か、ローカル再生成に載せる。
  G52 で機械可視化済み（`validate-tx-core.py` が各カードの `.tx-v13-trap` 欠落を報告）。
- 良好（trap 5/5・BASIS 充実）：361, 363-365, 386-400。

---

## 第2-3項 正誤表リデザイン 未移行ワークリスト（2026-07-07・v13m完遂時に確定）

v13m の合意（横串trap・BASIS中身・相互リンク・🗝記憶のフック）は全 corpus 完遂＋ERROR ゲート化済み。
残るのは **v13.1.0 第2-3項（正誤表リデザイン）＝旧 v13.0.0 の 26本**（`validate-tx-core` の G50 WARNING
＝nb-badge／computeInlineScore／data-brief-mark 欠で毎回可視化される）。**当面 WARNING**（spec §10）。

- **対象26本**：刑TX089 125 174 218 256 290 291 292 293 294 295 296 297 298 299 300 301 302 355 356 357 358 359 360 420 ／ 刑訴TX001
- **なぜローカル別作業か**：決定論ツール `scripts/tx-lex-verdict-redesign.py` は CSS＋エンジン（成績 computeInlineScore）
  までしか自動注入できず、**nb-badge（体系マップ各ノードの✍規範核1文）と data-brief-mark（正誤表各行の印付き原文要約）は
  問題ごとの手執筆**（26本×約10項目＝約260項目）。土台だけ流すと印付き原文が空で正誤表が劣化するため、
  **ツール注入＋規範核/印付き原文の執筆をセットで**行う。
- **手順（1本ずつ）**：
  1. `python -X utf8 scripts/tx-lex-verdict-redesign.py <target_lex>`（CSS＋成績エンジン注入・冪等）。
  2. 体系マップ各ノードに `.nb-badge`＋規範核1文（転用可能な規範の核・11〜14字）を鋳造（`tx-lex-v131-author.py` 併用可）。
  3. 正誤表各行に `data-brief-mark`（誤り核語句に下線＋→正解／正しさの決め手に緑下線の要約版）を鋳造。
  4. `validate-tx-core.py` の G50 WARNING（nb-badge/score/brief-mark）が消えたことを確認。
- 完了本は上記リストから消し込む。全26本完了で第2-3項も ERROR 化できる（G54 と同じ昇格手順）。

---

## 360-445 帯 v13.1.0 化 台帳（2026-07-07・機械監査 `tx-classify-type.py`）

360-400 は v13 化済み帯。**未移行の実体は 401-445 に集中**。内訳：

| 区分 | 本数 | 番号 | 経路 | PDF |
|---|---|---|---|---|
| **特殊型 backlog** | 16 | 406 408 411 412 415 416 418 424 426 427 428 429 430 431 433 439 | **R 再生成（PDF→v13）**。ラッパー非対応 | **Drive のみ**＝要回収 |
| **非特殊 v11 backlog** | 28→24 | 401-404(済) 405-445 の非特殊 | **決定論ラッパー `tx-lex-v11-to-v13.py`**（手順＝`docs/tx-v11-to-v13-slots-guide.md`） | **不要**（既存 v11 本文が素材） |
| 正誤表リデザイン待ち | 2 | 360 420 | verdict-redesign（上記26本リスト） | 不要 |

- **401-404 完了（2026-07-07・リモート）**：`tx-lex-v11-to-v13.py` で v13.1.0 化（401=国家的法益サーベイ型／402-404=逃走の罪）。スロット手順は `docs/tx-v11-to-v13-slots-guide.md`（落とし穴 G32/G34・G50・kihan字数を明文化）。以下は 401 の例：国家的法益
  5罪サーベイ型のため体系マップは保護法益3系統で手組み（pbox は非標準マップで SKIP＝正常・版判定は
  nb-badge＋brief-mark の実体で成立）。全ゲート PASS。**参照用 slots/data の雛形化**を次の改善候補に。
- **サーベイ型の要点（改善所見）**：400 番台は「1問に別罪5つ」のサーベイ型が多く、体系マップは
  客体三分でなく **保護法益タクソノミー**を手組みする前提を標準化する（`slots['nodes']` を明示指定・
  `subject` は gizou/houka 以外＝pbox SKIP でよい）。cross は3軸マトリクス、mark/gist/hook/trap/kihan は
  既存 v11 の分析を素材に鋳造。
- **特殊型 16本の前提＝PDF 回収**：repo のローカル PDF は 000〜330 まで。**360-445 は Drive のみ**なので、
  R 再生成の前に `Drive「1 TX_短答/001_刑法/抽出PDF」→ inputs/000_TX/001_刑法/` へ回収する。
  以後 TX PDF は削除しない方針（CLAUDE.md §9「TX 入力 PDF の保持」2026-07-07）。
